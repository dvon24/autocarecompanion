/* eslint-disable @typescript-eslint/no-require-imports */
const crypto = require('node:crypto'); const fs = require('node:fs'); const readline = require('node:readline'); const { BULLETIN_INVENTORY, CAMPAIGNS, DOCUMENTS, PDF_SOURCES, RECALL_FILES, RECALL_INVENTORY, RELEVANT_DOCUMENT_COUNTS, SOURCE_FILES } = require('./build-land-rover-range-rover-velar-adjudication');
const aliases = new Set(BULLETIN_INVENTORY.modelAliases); const patterns = Object.freeze({ def: /AdBlue|\bDEF\b|urea|crystalli[sz]ation|selective catalytic|\bSCR\b/i, door: /deployable door handle|door handle|flush.{0,20}handle|handle.{0,20}deploy/i, touch: /Touch Pro Duo|touchscreen|touch screen|infotainment|black screen|screen.*blank|display.*blank|lower screen|HVAC.*screen/i });
function stable(value) { if (Array.isArray(value)) return value.map(stable); if (value && typeof value === 'object') return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stable(value[key])])); return value; } function equal(left, right) { return JSON.stringify(stable(left)) === JSON.stringify(stable(right)); }
function parseCsv(line) { const values = []; let value = ''; let quoted = false; for (let index = 0; index < line.length; index += 1) { const char = line[index]; if (char === '"') { if (quoted && line[index + 1] === '"') { value += '"'; index += 1; } else quoted = !quoted; } else if (char === ',' && !quoted) { values.push(value); value = ''; } else value += char; } values.push(value); return values; }
async function readLines(file, onLine) { const reader = readline.createInterface({ input: fs.createReadStream(file), crlfDelay: Infinity }); for await (const line of reader) onLine(line); }
async function hashFile(file) { const hash = crypto.createHash('sha256'); await new Promise((resolve, reject) => { const stream = fs.createReadStream(file); stream.on('data', (chunk) => hash.update(chunk)); stream.on('end', resolve); stream.on('error', reject); }); return hash.digest('hex'); }
async function verifyFiles(files) { const results = []; for (const source of files) { const stat = fs.statSync(source.path); const sha256 = await hashFile(source.path); if (stat.size !== source.length || sha256 !== source.sha256) throw new Error(`${source.period}: source file drift`); results.push({ period: source.period, bytes: stat.size, sha256 }); } return results; }
async function verifyCommunications() { const periodCounts = {}; const matched = Object.fromEntries(Object.keys(patterns).map((key) => [key, new Set()])); const summariesById = new Map(); for (const source of SOURCE_FILES) { let count = 0; let first = true; await readLines(source.path, (line) => { if (first) { first = false; return; } const [id, make, model, _year, summary] = parseCsv(line); if (make !== 'LAND ROVER' || !aliases.has(model)) return; count += 1; summariesById.set(id, summary); for (const [name, pattern] of Object.entries(patterns)) if (pattern.test(summary)) matched[name].add(id); }); periodCounts[source.period] = count; } for (const ids of Object.values(DOCUMENTS)) for (const id of ids) if (!summariesById.has(id)) throw new Error(`cited document ${id} missing`); const relevantDocumentCounts = Object.fromEntries(Object.entries(matched).map(([name, ids]) => [name, ids.size])); const totalRows = Object.values(periodCounts).reduce((sum, count) => sum + count, 0); if (!equal(periodCounts, BULLETIN_INVENTORY.periodCounts) || totalRows !== 1138 || !equal(relevantDocumentCounts, RELEVANT_DOCUMENT_COUNTS)) throw new Error(`communication inventory drift: ${JSON.stringify(relevantDocumentCounts)}`); return { periodCounts, relevantDocumentCounts, totalRows }; }
async function verifyRecalls() { const periodCounts = {}; const rows = []; for (const source of RECALL_FILES) { let count = 0; await readLines(source.path, (line) => { const fields = line.split('\t'); if (fields[2] !== 'LAND ROVER' || !aliases.has(fields[3])) return; count += 1; rows.push({ campaign: fields[1], model: fields[3], year: fields[4] }); }); periodCounts[source.period] = count; } const campaigns = [...new Set(rows.map((row) => row.campaign))].sort(); const uniqueCampaignYearModelRows = new Set(rows.map((row) => `${row.campaign}|${row.model}|${row.year}`)).size; if (rows.length !== 502 || uniqueCampaignYearModelRows !== 18 || !equal(campaigns, CAMPAIGNS) || !equal(periodCounts, { pre: 0, post: 502 })) throw new Error('recall inventory drift'); return { periodCounts, totalRows: rows.length, uniqueCampaignYearModelRows, campaignCount: campaigns.length, campaigns }; }
async function verifyPdf(name, expected) { const response = await fetch(expected.url); if (!response.ok) throw new Error(`${response.status} ${expected.url}`); const buffer = Buffer.from(await response.arrayBuffer()); const sha256 = crypto.createHash('sha256').update(buffer).digest('hex'); const contentType = response.headers.get('content-type') || ''; if (buffer.length !== expected.bytes || sha256 !== expected.sha256 || !/application\/pdf/i.test(contentType) || !buffer.subarray(0, 5).equals(Buffer.from('%PDF-'))) throw new Error(`${name}: PDF/hash mismatch`); return { name, url: expected.url, bytes: buffer.length, sha256, contentType }; }
async function fetchJson(url) { const response = await fetch(url); if (!response.ok) throw new Error(`${response.status} ${url}`); return response.json(); }
async function verifyCampaign(campaign) { const url = `https://api.nhtsa.gov/recalls/campaignNumber?campaignNumber=${campaign}`; const body = await fetchJson(url); const text = JSON.stringify(body.results || []); if (!body.Count || !new RegExp(campaign, 'i').test(text) || !(body.results || []).some((row) => row.Make === 'LAND ROVER' && row.Model === 'RANGE ROVER VELAR')) throw new Error(`${campaign}: Velar campaign lookup mismatch`); return { campaign, count: body.Count }; }
async function main() { const [communicationFiles, recallFiles, communications, recalls] = await Promise.all([verifyFiles(SOURCE_FILES),verifyFiles(RECALL_FILES),verifyCommunications(),verifyRecalls()]); const pdfResults = []; for (const [name, expected] of Object.entries(PDF_SOURCES)) pdfResults.push(await verifyPdf(name, expected)); const campaignResults = []; for (const campaign of CAMPAIGNS) campaignResults.push(await verifyCampaign(campaign)); console.log(JSON.stringify({ passed: true, communicationFiles, recallFiles, communications, recalls, pdfResults, campaignResults }, null, 2)); }
main().catch((error) => { console.error(error); process.exitCode = 1; });
