/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const test = require('node:test');
const { IDS } = require('./build-jeep-compass-adjudication'); const { PACKET, SNAPSHOT, validatePacket } = require('./validate-jeep-compass-adjudication');
const packet = JSON.parse(fs.readFileSync(PACKET, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8'));
test('Compass packet passes the proposal-only safety contract', () => { assert.deepEqual(validatePacket(packet, snapshot), []); assert.deepEqual(packet.summary, { rewrite_same_identity: 0, keep_published_pending_source: 9, total: 9 }); });
test('all nine Compass rows remain byte-for-byte frozen', () => { for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.deepEqual(row.proposal, row.before); assert.equal(row.proposalSha256, row.beforeSha256); assert.deepEqual(row.changedFields, []); } });
test('packet covers the complete frozen Compass set', () => { const expected = snapshot.records.filter((row) => row.make === 'Jeep' && row.model === 'Compass').map((row) => row.id).sort(); assert.deepEqual(packet.rows.map((row) => row.id).sort(), expected); assert.deepEqual(Object.values(IDS).sort(), expected); });
test('narrow recalls and unsafe fitment claims remain explicit', () => { const byId = new Map(packet.rows.map((row) => [row.id, row])); assert.match(byId.get(IDS.oilPump).reason, /17V670.*2018.*2017-2022/i); assert.match(byId.get(IDS.electrical).reason, /18V524.*2018.*2017-2021/i); assert.match(byId.get(IDS.nineSpeed).reason, /8-speed.*9-speed|9-speed.*8-speed/i); assert.match(byId.get(IDS.windshield).reason, /silicone.*glazing|glazing.*silicone/i); });
