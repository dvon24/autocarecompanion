/* eslint-disable @typescript-eslint/no-require-imports */
const crypto = require('node:crypto'); const fs = require('node:fs'); const { analyze } = require('./analyze-lincoln-corsair-sources'); const { BULLETIN_INVENTORY, PDF_SOURCES, RECALL_INVENTORY } = require('./build-lincoln-corsair-adjudication'); const { RECALL_FILES, SOURCE_FILES } = require('./lincoln-adjudication-utils');
function hash(buffer) { return crypto.createHash('sha256').update(buffer).digest('hex'); }
async function verifyFiles(files) { const out = []; for (const source of files) { const buffer = await fs.promises.readFile(source.path); if (buffer.length !== source.length || hash(buffer) !== source.sha256) throw new Error(`${source.period}: source file drift`); out.push({ period: source.period, bytes: buffer.length, sha256: source.sha256 }); } return out; }
function verifyPdfBuffer(buffer, source, label) { const sha256 = hash(buffer); if (!buffer.subarray(0, 5).equals(Buffer.from('%PDF-')) || buffer.length !== source.bytes || sha256 !== source.sha256) throw new Error(`${source.title} ${label}: PDF/hash mismatch`); return { label, bytes: buffer.length, sha256 }; }
async function verifyPdf(source) { const local = verifyPdfBuffer(fs.readFileSync(source.localPath), source, 'local'); const response = await fetch(source.url, { headers: { accept: 'application/pdf', 'user-agent': 'au7o-known-issue-source-audit/1.0' } }); if (!response.ok) throw new Error(`${source.url}: status ${response.status}`); const remote = verifyPdfBuffer(Buffer.from(await response.arrayBuffer()), source, 'remote'); return { url: source.url, pages: source.pages, contentType: response.headers.get('content-type') || '', local, remote }; }
async function verifyAllStaticLinks(analysis) { const urls = [...new Set(analysis.checks.filter((check) => /^https:\/\/static\.nhtsa\.gov\/.*\.pdf$/i.test(check.url)).map((check) => check.url))]; const results = []; for (const url of urls) { const response = await fetch(url, { headers: { accept: 'application/pdf', 'user-agent': 'au7o-known-issue-source-audit/1.0' } }); const buffer = Buffer.from(await response.arrayBuffer()); if (!response.ok || !buffer.subarray(0, 5).equals(Buffer.from('%PDF-'))) throw new Error(`${url}: not a live PDF (${response.status})`); results.push({ url, status: response.status, bytes: buffer.length, sha256: hash(buffer) }); } return results; }
async function main() { const analysis = await analyze(); if (!analysis.passed) throw new Error(`source analyzer failed: ${JSON.stringify(analysis.problems)}`); if (analysis.communicationTotal !== BULLETIN_INVENTORY.totalRows || analysis.recallTotal !== RECALL_INVENTORY.totalRows || analysis.campaignCount !== RECALL_INVENTORY.campaignCount) throw new Error('Corsair inventory drift'); const [communicationFiles, recallFiles, pdfPairs, liveStaticLinks] = await Promise.all([verifyFiles(SOURCE_FILES), verifyFiles(RECALL_FILES), Promise.all(Object.entries(PDF_SOURCES).map(async ([key, source]) => [key, await verifyPdf(source)])), verifyAllStaticLinks(analysis)]); if (liveStaticLinks.length !== 4) throw new Error(`expected four proposed static NHTSA PDFs, found ${liveStaticLinks.length}`); console.log(JSON.stringify({ passed: true, inventory: { communicationCounts: analysis.communicationCounts, communicationTotal: analysis.communicationTotal, recallCounts: analysis.recallCounts, recallTotal: analysis.recallTotal, campaignCount: analysis.campaignCount, citationVerdicts: analysis.verdictCounts }, communicationFiles, recallFiles, pdfs: Object.fromEntries(pdfPairs), liveStaticLinkCount: liveStaticLinks.length, liveStaticLinks }, null, 2)); }
if (require.main === module) main().catch((error) => { console.error(error); process.exitCode = 1; });
