/* eslint-disable @typescript-eslint/no-require-imports */
const crypto = require('node:crypto');
const fs = require('node:fs');
const readline = require('node:readline');
const { EXPECTED_COMPLETE_RECALL_INVENTORY, EXPECTED_FLAT_RECALL_INVENTORY, EXPECTED_PRE_2010_RECALL_INVENTORY, FLAT_RECALL_SOURCE, MFR_COMMUNICATIONS_SOURCE } = require('./build-kia-soul-adjudication');

function parseCsvLine(line) { const fields=[]; let field=''; let quoted=false; for(let i=0;i<line.length;i+=1){const char=line[i];if(quoted){if(char==='"'){if(line[i+1]==='"'){field+='"';i+=1;}else quoted=false;}else field+=char;}else if(char===','){fields.push(field);field='';}else if(char==='"')quoted=true;else field+=char;}fields.push(field);return fields; }
function normalizedInventory(map) { return Object.fromEntries([...map.entries()].sort().map(([campaign, years]) => [campaign, [...years].sort((a,b)=>a-b)])); }
async function inspectCsv(file, expected) { const hash=crypto.createHash('sha256');const input=fs.createReadStream(file);input.on('data',(chunk)=>hash.update(chunk));const rl=readline.createInterface({input,crlfDelay:Infinity});let headers;let soulRows=0;let lines=0;const documentIds=new Set();for await(const line of rl){lines+=1;const fields=parseCsvLine(line);if(!headers){headers=fields;continue;}const row=Object.fromEntries(headers.map((header,index)=>[header,fields[index]||'']));if(row.Make==='KIA'&&row.Model==='SOUL'){soulRows+=1;documentIds.add(row['TSB/Document ID']);}}const sha256=hash.digest('hex');return{file,lines,sha256,expectedSha256:expected.sha256,soulRows,expectedSoulRows:expected.expectedSoulRows,documentIds:[...documentIds].sort(),passed:sha256===expected.sha256&&soulRows===expected.expectedSoulRows}; }
async function inspectFlat(file, expected, expectedInventory) { const hash=crypto.createHash('sha256');const input=fs.createReadStream(file);input.on('data',(chunk)=>hash.update(chunk));const rl=readline.createInterface({input,crlfDelay:Infinity});let lines=0;let soulRows=0;const campaigns=new Map();for await(const line of rl){lines+=1;const c=line.split('\t');if((c[2]||'').toUpperCase()!=='KIA'||(c[3]||'').toUpperCase()!=='SOUL')continue;soulRows+=1;const years=campaigns.get(c[1])||new Set();years.add(Number(c[4]));campaigns.set(c[1],years);}const sha256=hash.digest('hex');const inventory=normalizedInventory(campaigns);return{file,lines,sha256,expectedSha256:expected.sha256,soulRows,expectedSoulRows:expected.expectedSoulRows,inventory,expectedInventory,passed:sha256===expected.sha256&&soulRows===expected.expectedSoulRows&&JSON.stringify(inventory)===JSON.stringify(expectedInventory)}; }
async function main(){const args=new Map(process.argv.slice(2).map((value)=>{const index=value.indexOf('=');return[value.slice(2,index),value.slice(index+1)];}));const csv=[];for(const[key,expected]of Object.entries(MFR_COMMUNICATIONS_SOURCE.files)){const file=args.get(key);if(!file)throw new Error(`missing --${key}=path`);csv.push(await inspectCsv(file,expected));}const preFile=args.get('pre-flat');const postFile=args.get('post-flat');if(!preFile||!postFile)throw new Error('missing flat recall paths');const pre=await inspectFlat(preFile,FLAT_RECALL_SOURCE.pre2010,EXPECTED_PRE_2010_RECALL_INVENTORY);const post=await inspectFlat(postFile,FLAT_RECALL_SOURCE.post2010,EXPECTED_FLAT_RECALL_INVENTORY);const totalSoulRows=csv.reduce((sum,item)=>sum+item.soulRows,0);const documentIds=new Set(csv.flatMap((item)=>item.documentIds));const missingRequiredDocumentIds=MFR_COMMUNICATIONS_SOURCE.requiredDocumentIds.filter((id)=>!documentIds.has(id));const complete=Object.fromEntries(Object.entries({...pre.inventory,...post.inventory}).sort());const completePassed=JSON.stringify(complete)===JSON.stringify(EXPECTED_COMPLETE_RECALL_INVENTORY);const passed=csv.every((item)=>item.passed)&&pre.passed&&post.passed&&totalSoulRows===MFR_COMMUNICATIONS_SOURCE.totalExpectedSoulRows&&missingRequiredDocumentIds.length===0&&completePassed;console.log(JSON.stringify({passed,checkedOn:'2026-08-08',totalSoulRows,expectedTotalSoulRows:MFR_COMMUNICATIONS_SOURCE.totalExpectedSoulRows,missingRequiredDocumentIds,manufacturerCommunications:csv,pre2010Recall:pre,post2010Recall:post,completeRecall:{inventory:complete,expectedInventory:EXPECTED_COMPLETE_RECALL_INVENTORY,campaignCount:Object.keys(complete).length,passed:completePassed}},null,2));if(!passed)process.exitCode=1;}
if(require.main===module)main().catch((error)=>{console.error(error);process.exitCode=1;});
module.exports={inspectCsv,inspectFlat,normalizedInventory,parseCsvLine};
