/* eslint-disable @typescript-eslint/no-require-imports */
const {CAMPAIGN_SOURCES,EXPECTED_CAMPAIGNS}=require('./build-kia-sorento-adjudication');
async function fetchWithRetries(url,attempts=4){let last;for(let attempt=1;attempt<=attempts;attempt+=1){try{const response=await fetch(url,{redirect:'follow',signal:AbortSignal.timeout(90000),headers:{'user-agent':'Mozilla/5.0 (compatible; au7o-source-audit/1.0)'}});if(response.ok)return response;last=new Error(`${response.status} ${response.statusText}`);}catch(error){last=error;}if(attempt<attempts)await new Promise((resolve)=>setTimeout(resolve,attempt*1200));}throw last;}
async function verifyCampaign(key,url){const response=await fetchWithRetries(url);const body=await response.json();const rows=(body.results||[]).filter((row)=>row.Model==='SORENTO');const years=[...new Set(rows.map((row)=>Number(row.ModelYear)))].sort((a,b)=>a-b);const expected=EXPECTED_CAMPAIGNS[key];const combined=JSON.stringify(rows);const missingMarkers=expected.markers.filter((marker)=>!combined.toLowerCase().includes(marker.toLowerCase()));return{key,url,status:response.status,sorentoRows:rows.length,expectedRows:expected.rows,years,expectedYears:expected.years,missingMarkers,passed:rows.length===expected.rows&&JSON.stringify(years)===JSON.stringify(expected.years)&&missingMarkers.length===0};}
async function main(){const campaigns=[];for(const[key,url]of Object.entries(CAMPAIGN_SOURCES))campaigns.push(await verifyCampaign(key,url));const passed=campaigns.every((item)=>item.passed);console.log(JSON.stringify({passed,checkedOn:'2026-08-08',campaigns},null,2));if(!passed)process.exitCode=1;}
if(require.main===module)main().catch((error)=>{console.error(error);process.exitCode=1;});module.exports={fetchWithRetries,verifyCampaign};
