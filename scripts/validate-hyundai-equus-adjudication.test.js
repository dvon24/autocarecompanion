/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path'); const test = require('node:test'); const { IDS } = require('./build-hyundai-equus-adjudication'); const { validatePacket } = require('./validate-hyundai-equus-adjudication'); const ROOT = path.resolve(__dirname, '..'); const packet = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', 'known-issue-hyundai-equus-adjudication-2026-08-06.json'), 'utf8')); const snapshot = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', '_hyundai-deeplink-snapshot-2026-08-06.json'), 'utf8'));
test('Equus hold packet passes the complete safety contract', () => { assert.deepEqual(validatePacket(packet, snapshot, packet.source.snapshotSha256), []); });
test('all four Equus rows remain byte-for-byte frozen', () => { assert.equal(packet.rows.length, 4); for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.equal(row.beforeSha256, row.proposalSha256, row.id); assert.deepEqual(row.changedFields, [], row.id); } });
test('packet covers every frozen Equus ID exactly once', () => { const expected = snapshot.records.filter((row) => row.model === 'Equus').map((row) => row.id).sort(); const actual = packet.rows.map((row) => row.id).sort(); assert.deepEqual(actual, expected); assert.equal(new Set(actual).size, 4); });
test('air-spring bulletin is mismatch evidence only', () => { const row = packet.rows.find((item) => item.id === IDS.airSuspension); assert.equal(row.beforeSha256, row.proposalSha256); assert.equal(row.evidence.length, 1); assert.match(row.reason, /does not establish/); });
