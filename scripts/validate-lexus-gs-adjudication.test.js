/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const test = require('node:test'); const assert = require('node:assert/strict'); const { clone } = require('./lexus-adjudication-utils'); const { IDS, SNAPSHOT, buildPacket } = require('./build-lexus-gs-adjudication'); const { validatePacket } = require('./validate-lexus-gs-adjudication'); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8'));
test('GS packet passes the full three-page safety contract', () => { assert.deepEqual(validatePacket(buildPacket(snapshot), snapshot), []); });
test('validator rejects indexed identity, engine and severity drift', () => { const packet = buildPacket(snapshot); packet.rows[0].proposal.title = 'Changed'; packet.rows[0].proposal.engines = ['Guess']; packet.rows[0].proposal.severity = 'critical'; const errors = validatePacket(packet, snapshot); assert.ok(errors.some((error) => /proposal drift|immutable title/.test(error))); assert.ok(errors.some((error) => /immutable engines/.test(error))); assert.ok(errors.some((error) => /severity/.test(error))); });
test('validator rejects archives and guessed commerce', () => { const packet = buildPacket(snapshot); packet.rows[1].proposal.status = 'archived'; packet.rows[1].proposal.fixParts = [{ oemPartNumber: 'GUESS' }]; assert.ok(validatePacket(packet, snapshot).some((error) => /proposal drift|publication\/source\/severity|derived-data\/commerce/.test(error))); });
test('validator rejects hydraulic service and aftermarket rack prescriptions', () => { const packet = clone(buildPacket(snapshot)); const row = packet.rows.find((item) => item.id === IDS.steering); row.proposal.solution += ' Add power steering fluid, perform a power steering fluid flush, and install a Cardone rack.'; assert.ok(validatePacket(packet, snapshot).some((error) => /unsupported prescription/.test(error))); });
test('validator rejects guessed water-pump bundle and fitment', () => { const packet = clone(buildPacket(snapshot)); const row = packet.rows.find((item) => item.id === IDS.pump); row.proposal.solution += ' Install Aisin WPT-190 and replace the thermostat together.'; assert.ok(validatePacket(packet, snapshot).some((error) => /unsupported prescription/.test(error))); });
