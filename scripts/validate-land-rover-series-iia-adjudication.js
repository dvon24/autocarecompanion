/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const { FULL_RECORD_FIELDS, fullRecord, hashValue, normalizedFileHash, stableValue } = require('./land-rover-adjudication-utils'); const { BULLETIN_INVENTORY, COMPLETE_MANUAL_REFERENCE, EARLY_MANUAL_SOURCE, OUTPUT: PACKET, RECALL_INVENTORY, REVIEW_DATE, SNAPSHOT, buildPacket, evidenceFor, primaryCitations, proposalFor, sectionFor } = require('./build-land-rover-series-iia-adjudication');
const EXPECTED_COUNT = 62; const EXPECTED_SUMMARY = { targeted_safety_cleanup_pending_source: 62, total: 62 }; const EXPECTED_SECTION_COUNTS = { 'A/AA': 8, C: 6, D: 2, E: 1, F: 5, G: 5, H: 6, J: 3, L: 1, M: 3, P: 8, Q: 1, R: 12, T: 1 }; const IMMUTABLE_FIELDS = ['make','model','years','trims','engines','category','title','status']; const REQUIRED_OBSERVATIONS = ['series-iia-no-exact-federal-records','series-iia-early-manual-bound','series-iia-complete-manual-missing','series-iia-secondary-citations-not-promoted','series-iia-prescriptive-guidance-held','series-iia-no-unverified-commerce','all-series-iia-pages-preserved']; const PUBLIC_EARLY_MANUAL = Object.fromEntries(Object.entries(EARLY_MANUAL_SOURCE).filter(([key]) => key !== 'localPath')); const UNSAFE_ADVICE = /\b(?:DOT\s*5|body filler|disc[- ]brake conversion|dual[- ](?:line|circuit)|24-spline|high ZDDP|silicone spray|brass sleeve|Cunifer|copper-nickel|hot-dip galvani[sz]e|alternator conversion|parabolic springs|polyurethane bushes|overdrive kit|electronic ignition)\b/i;
function equal(left, right) { return JSON.stringify(stableValue(left)) === JSON.stringify(stableValue(right)); }
function validatePacket(packet, snapshot, expectedSnapshotSha256 = normalizedFileHash(SNAPSHOT)) { const errors = []; const modelRows = snapshot.records.filter((row) => row.make === 'Land Rover' && row.model === 'Series IIA').sort((a, b) => a.id.localeCompare(b.id)); const frozenById = new Map(modelRows.map((row) => [row.id, row])); const ids = packet.rows?.map((row) => row.id) || [];
  if (packet.status !== 'proposal-only' || packet.requiresIndependentApproval !== true || packet.auditStage !== 'model-primary-source-adjudication') errors.push('packet safety status mismatch'); if (packet.make !== 'Land Rover' || packet.model !== 'Series IIA') errors.push('packet scope mismatch'); if (packet.applicationGate?.status !== 'blocked' || !equal(packet.applicationGate?.blockerRecordIds, modelRows.map((row) => row.id))) errors.push('application blocker set mismatch'); if (packet.source?.snapshotSha256 !== expectedSnapshotSha256 || packet.source?.snapshotHash !== snapshot.snapshotHash) errors.push('snapshot binding mismatch'); if (packet.source?.modelRecordCount !== EXPECTED_COUNT || modelRows.length !== EXPECTED_COUNT || ids.length !== EXPECTED_COUNT || new Set(ids).size !== EXPECTED_COUNT || !equal(ids, modelRows.map((row) => row.id))) errors.push('Series IIA frozen-row coverage mismatch'); if (!equal(packet.summary, EXPECTED_SUMMARY) || !equal(packet.manualSectionCounts, EXPECTED_SECTION_COUNTS)) errors.push('summary or manual-section count mismatch'); if (!equal(packet.earlyFactoryManual, PUBLIC_EARLY_MANUAL) || !equal(packet.completeManualReference, COMPLETE_MANUAL_REFERENCE) || packet.completeManualReference.contentAvailableInEvidenceSet !== false) errors.push('manual evidence boundary mismatch'); if (!equal(packet.manufacturerCommunications, BULLETIN_INVENTORY) || !equal(packet.recallInventory, RECALL_INVENTORY) || BULLETIN_INVENTORY.totalRows !== 0 || RECALL_INVENTORY.totalRows !== 0) errors.push('federal source inventory mismatch'); if (!equal(packet.mappedCampaigns, []) || !equal(packet.deferredCampaigns, [])) errors.push('campaign partition mismatch');
  for (const row of packet.rows || []) { const frozen = frozenById.get(row.id); if (!frozen) { errors.push(`${row.id}: unknown ID`); continue; } const before = fullRecord(frozen); const expectedProposal = proposalFor(frozen); if (row.action !== 'targeted_safety_cleanup_pending_source' || row.commerceDecision !== 'blocked-no-exact-fitment-no-retail-part') errors.push(`${row.id}: decision mismatch`); if (!equal(row.manualSection, sectionFor(frozen))) errors.push(`${row.id}: manual-section mismatch`); if (!equal(row.before, before) || row.beforeSha256 !== hashValue(before)) errors.push(`${row.id}: before drift`); if (!equal(row.proposal, expectedProposal) || row.proposalSha256 !== hashValue(expectedProposal)) errors.push(`${row.id}: proposal drift`); const changedFields = FULL_RECORD_FIELDS.filter((field) => hashValue(before[field]) !== hashValue(expectedProposal[field])); if (!equal(row.changedFields, changedFields) || !row.changedFields.length) errors.push(`${row.id}: changed-field drift`); if (!equal(row.evidence, evidenceFor(frozen)) || row.evidence.length !== 3) errors.push(`${row.id}: evidence drift`); for (const field of IMMUTABLE_FIELDS) if (!equal(row.proposal[field], before[field])) errors.push(`${row.id}: immutable ${field} drift`); for (const field of FULL_RECORD_FIELDS) if (!Object.prototype.hasOwnProperty.call(row.before, field) || !Object.prototype.hasOwnProperty.call(row.proposal, field)) errors.push(`${row.id}: missing ${field}`); if (row.proposal.status !== 'published' || /^Archived\s*-/i.test(row.proposal.title) || row.proposal.humanApproved !== false || row.proposal.reportCount !== 0 || row.proposal.source !== 'manual' || row.proposal.confidence !== 'low') errors.push(`${row.id}: publication/source drift`); if (row.proposal.reviewedOn !== REVIEW_DATE || row.proposal.contentUpdatedOn !== REVIEW_DATE || !row.proposal.contentUpdateSummary) errors.push(`${row.id}: review metadata drift`); if (!equal(row.proposal.symptoms, []) || !equal(row.proposal.affectedSystems, []) || !equal(row.proposal.dtcCodes, []) || !equal(row.proposal.communityRecommendations, []) || !equal(row.proposal.fixParts, []) || !equal(row.proposal.relatedIssueIds, [])) errors.push(`${row.id}: derived-data/commerce/relation drift`); if ([row.proposal.estimatedCostLow,row.proposal.estimatedCostHigh,row.proposal.typicalMileageLow,row.proposal.typicalMileageHigh].some((value) => value !== null)) errors.push(`${row.id}: cost/mileage remains`); if (!equal(row.proposal.citations, primaryCitations())) errors.push(`${row.id}: citation boundary mismatch`); if (!/No universal retail part or dealer-only remedy is asserted\./.test(row.proposal.solution)) errors.push(`${row.id}: explicit no-commerce marker missing`); if (UNSAFE_ADVICE.test(row.proposal.description) || UNSAFE_ADVICE.test(row.proposal.solution)) errors.push(`${row.id}: unsafe prescriptive advice survived`); if (/amazon\.com\/s\?k=|ebay\.com\/sch|rockauto\.com\/en\/partsearch|abcd1234efg|comments\/abcd12\//i.test(JSON.stringify(row.proposal))) errors.push(`${row.id}: search commerce or placeholder survived`); if (!(before.citations || []).length || (before.citations || []).some((citation) => /^https:\/\/(?:www\.nhtsa\.gov|static\.nhtsa\.gov|(?:www\.)?jaguarlandrover\.com|media\.jaguarlandrover\.com)\//i.test(citation.url || ''))) errors.push(`${row.id}: frozen citation classification mismatch`); if (!equal(before.fixParts, [])) errors.push(`${row.id}: unexpected frozen commerce`); }
  for (const code of REQUIRED_OBSERVATIONS) { const observation = packet.observations?.find((item) => item.code === code); if (!observation) errors.push(`missing observation ${code}`); else if (!equal(observation.recordIds, modelRows.map((row) => row.id))) errors.push(`${code}: record coverage mismatch`); } if (!equal(packet, buildPacket(snapshot))) errors.push('packet differs from deterministic rebuild'); return errors; }
if (require.main === module) { const packet = JSON.parse(fs.readFileSync(PACKET, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const errors = validatePacket(packet, snapshot); console.log(JSON.stringify({ passed: errors.length === 0, packetSha256: normalizedFileHash(PACKET), decisionCount: packet.rows?.length || 0, applicationGate: packet.applicationGate, errors }, null, 2)); if (errors.length) process.exitCode = 1; }
module.exports = { EXPECTED_COUNT, EXPECTED_SECTION_COUNTS, EXPECTED_SUMMARY, PACKET, REQUIRED_OBSERVATIONS, SNAPSHOT, validatePacket };
