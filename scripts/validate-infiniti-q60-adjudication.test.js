/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path'); const test = require('node:test'); const { IDS, SOURCES } = require('./build-infiniti-q60-adjudication'); const { validatePacket } = require('./validate-infiniti-q60-adjudication');
const ROOT = path.resolve(__dirname, '..'); const packet = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', 'known-issue-infiniti-q60-adjudication-2026-08-06.json'), 'utf8')); const snapshot = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'), 'utf8'));
test('Q60 hold packet passes the complete safety contract', () => assert.deepEqual(validatePacket(packet, snapshot, packet.source.snapshotSha256), []));
test('both indexed Q60 rows remain byte-for-byte frozen', () => { assert.equal(packet.rows.length, 2); for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.equal(row.beforeSha256, row.proposalSha256, row.id); assert.deepEqual(row.changedFields, [], row.id); assert.deepEqual(row.proposal, row.before, row.id); } });
test('packet covers every frozen Q60 ID exactly once', () => { const expected = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'Q60').map((row) => row.id).sort(); const actual = packet.rows.map((row) => row.id).sort(); assert.deepEqual(actual, expected); assert.equal(new Set(actual).size, 2); });
test('partial infotainment sources and unrelated steering evidence cannot rewrite', () => { const infotainment = packet.rows.find((row) => row.id === IDS.infotainment); assert.deepEqual(infotainment.evidence.map((item) => item.url), [SOURCES.diagnostic, SOURCES.q60Update]); assert.match(packet.rows.find((row) => row.id === IDS.steering).reason, /subjective/i); });
