/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const path = require('node:path'); const { clone, diffFields, fullRecord, hashValue, normalizedFileHash } = require('./land-rover-adjudication-utils'); const { RECALL_FILES, SOURCE_FILES } = require('./build-land-rover-range-rover-velar-adjudication');
const SNAPSHOT = path.resolve(__dirname, '..', 'data', '_land-rover-deeplink-snapshot-2026-08-08.json'); const OUTPUT = path.resolve(__dirname, '..', 'data', 'known-issue-land-rover-series-iii-adjudication-2026-08-08.json'); const REVIEW_DATE = '2026-08-08'; const NHTSA_DATASET_URL = 'https://www.nhtsa.gov/nhtsa-datasets-and-apis#manufacturer-communications'; const RECALL_DATASET_URL = 'https://www.nhtsa.gov/nhtsa-datasets-and-apis#recalls';
const MANUAL_SOURCE = Object.freeze({ title: 'Land Rover Series III Repair Operation Manual AKM3648, Edition 4', url: 'https://landrover.series2club.co.uk/wp-content/uploads/technical_library/Workshop_Manual_Series_III.pdf', localPath: 'C:/tmp/land-rover-series-iii-akm3648.pdf', pages: 372, bytes: 19995636, sha256: 'd4ed0bf531ae20ca159c37e0a8e91145af743e7c5812691bd2d28a87a3f248b7', publicationNumber: 'AKM3648', edition: '4' });
const MODEL_ALIASES = Object.freeze(['SERIES III','SERIES 3']); const BULLETIN_INVENTORY = Object.freeze({ source: NHTSA_DATASET_URL, modelAliases: MODEL_ALIASES, periodCounts: { '1995-1999': 0, '2000-2004': 0, '2005-2009': 0, '2010-2014': 0, '2015-2019': 0, '2020-2024': 0, '2025-2026': 0 }, totalRows: 0, sourceFiles: SOURCE_FILES.map(({ path: _path, ...source }) => source) }); const RECALL_INVENTORY = Object.freeze({ source: RECALL_DATASET_URL, modelAliases: MODEL_ALIASES, periodCounts: { pre: 0, post: 0 }, totalRows: 0, uniqueCampaignYearModelRows: 0, campaignCount: 0, campaigns: [], mappedCampaigns: [], deferredCampaigns: [], sourceFiles: RECALL_FILES.map(({ path: _path, ...source }) => source) });
const MANUAL_SECTIONS = Object.freeze({ engine: { code: '12', title: 'Engine operations' }, gearbox: { code: '37', title: 'Gearbox' }, rearAxle: { code: '51', title: 'Rear axle' }, frontAxle: { code: '54', title: 'Front axle and final drive' }, brakes: { code: '70', title: 'Brakes' }, body: { code: '76', title: 'Body' }, electrical: { code: '86', title: 'Electrical operations' } });
function sectionFor(row) { let section; if (row.category === 'engine') section = MANUAL_SECTIONS.engine; else if (row.category === 'transmission') section = MANUAL_SECTIONS.gearbox; else if (row.category === 'drivetrain') section = /rear-axle|half-shaft/.test(row.id) ? MANUAL_SECTIONS.rearAxle : MANUAL_SECTIONS.frontAxle; else if (row.category === 'brakes') section = MANUAL_SECTIONS.brakes; else if (row.category === 'body') section = MANUAL_SECTIONS.body; else if (row.category === 'electrical') section = MANUAL_SECTIONS.electrical; if (!section) throw new Error(`${row.id}: no AKM3648 section mapping for ${row.category}`); return section; }
function primaryCitations() { return [{ type: 'manual', title: MANUAL_SOURCE.title, url: MANUAL_SOURCE.url }, { type: 'nhtsa', title: 'NHTSA Manufacturer Communications datasets', url: NHTSA_DATASET_URL }, { type: 'nhtsa', title: 'NHTSA Recall datasets', url: RECALL_DATASET_URL }]; }
function contentFor(row) { const section = sectionFor(row); return { description: `This indexed Series III page remains under corrective review for a ${section.title.toLowerCase()} concern. Factory Repair Operation Manual AKM3648 confirms the applicable system and service operations, but it does not establish the existing prevalence, universal year/engine/wheelbase coverage, upgrade claims or replacement-part fitment. The complete frozen NHTSA manufacturer-communication and recall inventories contain no exact classic Series III model row, so the existing secondary-source narrative is not promoted as verified fact.`, solution: `Do not select a replacement part, fluid, adjustment, conversion or repair procedure from this page. Identify the chassis number, wheelbase, build year, engine, axle and installed component, then diagnose against AKM3648 section ${section.code} (${section.title}) and the matching factory parts catalogue. Safety-critical work requires inspection by a qualified classic Land Rover technician. No universal retail part or dealer-only remedy is asserted.`, summary: `Placed the secondary-source ${section.title.toLowerCase()} narrative on corrective hold, removed unverified repair/upgrade and commerce guidance, and bound the next review to AKM3648 section ${section.code}.` }; }
function proposalFor(row) { const proposal = clone(fullRecord(row)); const content = contentFor(row); proposal.description = content.description; proposal.solution = content.solution; proposal.confidence = 'low'; proposal.symptoms = []; proposal.affectedSystems = []; proposal.dtcCodes = []; proposal.estimatedCostLow = null; proposal.estimatedCostHigh = null; proposal.typicalMileageLow = null; proposal.typicalMileageHigh = null; proposal.citations = primaryCitations(); proposal.communityRecommendations = []; proposal.fixParts = []; proposal.humanApproved = false; proposal.reportCount = 0; proposal.source = 'manual'; proposal.lastReportedByOwners = ''; proposal.reviewedOn = REVIEW_DATE; proposal.contentUpdatedOn = REVIEW_DATE; proposal.contentUpdateSummary = content.summary; proposal.relatedIssueIds = []; return proposal; }
function evidenceFor(row) { const section = sectionFor(row); return ['Complete federal inventory: zero exact classic Series III rows across all seven frozen NHTSA manufacturer-communication periods and both frozen recall periods.', `The visually inspected 372-page factory Repair Operation Manual AKM3648 confirms section ${section.code} (${section.title}); the downloaded PDF is bound by byte length and SHA-256.`, 'The frozen page cites only secondary/community/commercial sources and its fitment-sensitive remedy is not treated as primary-source proof; no part, fluid, conversion or procedure is approved.']; }
function buildPacket(snapshot) { const rows = snapshot.records.filter((row) => row.make === 'Land Rover' && row.model === 'Series III').sort((a, b) => a.id.localeCompare(b.id)); const blockerIds = rows.map((row) => row.id); const decisions = rows.map((row) => { const before = fullRecord(row); const proposal = proposalFor(row); return { id: row.id, action: 'targeted_safety_cleanup_pending_source', commerceDecision: 'blocked-no-exact-fitment-no-retail-part', manualSection: sectionFor(row), evidence: evidenceFor(row), before, beforeSha256: hashValue(before), proposal, proposalSha256: hashValue(proposal), changedFields: diffFields(before, proposal) }; }); const sectionCounts = Object.fromEntries(Object.values(MANUAL_SECTIONS).map((section) => [section.code, rows.filter((row) => sectionFor(row).code === section.code).length]).filter(([, count]) => count)); return {
  schemaVersion: 1, status: 'proposal-only', auditStage: 'model-primary-source-adjudication', requiresIndependentApproval: true, generatedOn: REVIEW_DATE, make: 'Land Rover', model: 'Series III', completionStatement: 'All 25 frozen Series III pages are preserved and receive bounded corrective holds; complete factory manual AKM3648 is verified, but current prevalence, upgrade and fitment claims rely only on secondary sources.', applicationGate: { status: 'blocked', blockerRecordIds: blockerIds, reason: 'All 25 remedies are fitment- or safety-sensitive. AKM3648 confirms repair operations but not prevalence or universal fitment; independent review is required per record.' },
  safetyContract: ['No production write, deployment, archive, redirect, slug change, title change, category change, indexed-year change, trim change, engine change or new issue is authorized.','All 25 IDs, titles, categories, indexed year sets, trim sets, engine sets and publication states remain unchanged.','No part, fluid, adjustment, conversion or repair procedure is approved without exact factory-document and vehicle-configuration support.','The full frozen NHTSA inventory was replayed; zero exact classic Series III manufacturer-communication or recall rows exist.','AKM3648 is hash-bound as the complete primary technical source, but a repair manual is not evidence that a condition is common.'], source: { snapshotFile: 'data/_land-rover-deeplink-snapshot-2026-08-08.json', snapshotSha256: normalizedFileHash(SNAPSHOT), snapshotGeneratedAt: snapshot.generatedAt, snapshotHash: snapshot.snapshotHash, modelRecordCount: rows.length },
  observations: [{ code: 'series-iii-no-exact-federal-records', severity: 'source-gap', recordIds: blockerIds, detail: 'Zero exact classic Series III rows exist in the complete frozen NHTSA manufacturer-communication and recall inventories.' },{ code: 'series-iii-akm3648-bound', severity: 'primary-source', recordIds: blockerIds, detail: 'Factory Repair Operation Manual AKM3648 Edition 4 is visually inspected and bound by 372 pages, 19,995,636 bytes and SHA-256.' },{ code: 'series-iii-secondary-citations-not-promoted', severity: 'accuracy-safety', recordIds: blockerIds, detail: 'All 25 frozen pages rely exclusively on secondary, community or commercial citations; none is treated as prevalence or fitment proof.' },{ code: 'series-iii-prescriptive-guidance-held', severity: 'critical-correction', recordIds: blockerIds, detail: 'All 25 remedies are fitment- or safety-sensitive; proposals remove those instructions pending exact factory-manual review.' },{ code: 'series-iii-no-unverified-commerce', severity: 'commerce-safety', recordIds: blockerIds, detail: 'No guessed product, fluid, conversion kit or search-style deep link is introduced.' },{ code: 'all-series-iii-pages-preserved', severity: 'seo-safety', recordIds: blockerIds, detail: 'Every Series III ID, title, category, indexed year set, trim set, engine set and publication state remains preserved.' }], factoryManual: Object.fromEntries(Object.entries(MANUAL_SOURCE).filter(([key]) => key !== 'localPath')), manualSectionCounts: sectionCounts, manufacturerCommunications: BULLETIN_INVENTORY, recallInventory: RECALL_INVENTORY, mappedCampaigns: [], deferredCampaigns: [], summary: { targeted_safety_cleanup_pending_source: rows.length, total: rows.length }, rows: decisions,
}; }
if (require.main === module) { const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const packet = buildPacket(snapshot); fs.writeFileSync(OUTPUT, `${JSON.stringify(packet, null, 2)}\n`); console.log(JSON.stringify({ output: OUTPUT, rows: packet.rows.length, summary: packet.summary, manualSectionCounts: packet.manualSectionCounts, applicationGate: packet.applicationGate.status }, null, 2)); }
module.exports = { BULLETIN_INVENTORY, MANUAL_SECTIONS, MANUAL_SOURCE, MODEL_ALIASES, OUTPUT, RECALL_FILES, RECALL_INVENTORY, REVIEW_DATE, SNAPSHOT, SOURCE_FILES, buildPacket, contentFor, evidenceFor, primaryCitations, proposalFor, sectionFor };
