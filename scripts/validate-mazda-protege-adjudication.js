/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const { ALL_IDS, BLOCKER_IDS, FABRICATED_REPORT_COUNT_IDS, IDS, OTHER_SOURCES, OUTPUT, PDF_SOURCES, RETAIN_IDS, SNAPSHOT, buildPacket, citationsFor, commerceDecisionFor, identityConflictFor } = require('./build-mazda-protege-adjudication'); const { FULL_RECORD_FIELDS, diffFields, fullRecord, hashValue, stableValue } = require('./mazda-adjudication-utils'); const IMMUTABLE_FIELDS = Object.freeze(['make', 'model', 'years', 'trims', 'engines', 'category', 'title', 'severity', 'status', 'lastReportedByOwners', 'relatedIssueIds']); const ALLOWED_CHANGED_FIELDS = new Set(['description', 'solution', 'confidence', 'symptoms', 'affectedSystems', 'dtcCodes', 'estimatedCostLow', 'estimatedCostHigh', 'typicalMileageLow', 'typicalMileageHigh', 'citations', 'communityRecommendations', 'fixParts', 'humanApproved', 'reportCount', 'source', 'reviewedOn', 'contentUpdatedOn', 'contentUpdateSummary']); function equal(a, b) { return JSON.stringify(stableValue(a)) === JSON.stringify(stableValue(b)); } function prose(row) { return `${row?.description || ''} ${row?.solution || ''} ${(row?.symptoms || []).join(' ')}`; } function searchStyle(url) { return /[?&](?:q|query|search|keyword)=|\/search(?:\/|\?|$)|\/s\?/i.test(String(url)); }
function validatePacket(packet, snapshot) { const errors = []; const deterministic = buildPacket(snapshot); const expected = snapshot.records.filter((row) => row.make === 'Mazda' && row.model === 'Protege').sort((a, b) => a.id.localeCompare(b.id)); const expectedById = new Map(expected.map((row) => [row.id, row])); const rows = Array.isArray(packet.rows) ? packet.rows : []; const ids = rows.map((row) => row.id); const fabricated = new Set(FABRICATED_REPORT_COUNT_IDS); const retained = new Set(RETAIN_IDS); const expectedSummary = { retain_indexed_identity_and_accuracy_cleanup: 1, hold_indexed_identity_and_accuracy_cleanup_pending_identity_policy: 9, fabricated_report_counts_proposed_zero: 2, total: 10 };
  if (!equal(packet, deterministic)) errors.push('packet does not exactly match deterministic frozen-snapshot build'); if (packet.status !== 'proposal-only' || packet.requiresIndependentApproval !== true || packet.applicationGate?.status !== 'blocked') errors.push('packet must remain blocked proposal-only'); if (packet.make !== 'Mazda' || packet.model !== 'Protege') errors.push('wrong make/model'); if (expected.length !== 10 || rows.length !== 10 || new Set(ids).size !== 10) errors.push('Protege coverage must be 10/10 unique rows'); if (!equal([...ids].sort(), ALL_IDS) || !equal([...ids].sort(), expected.map((row) => row.id).sort())) errors.push('packet IDs do not exactly match frozen Protege snapshot'); if (!equal(packet.applicationGate?.blockerRecordIds || [], BLOCKER_IDS) || !equal(packet.summary, expectedSummary)) errors.push('blocker IDs or summary drifted'); if (!packet.safetyContract?.some((line) => /0\+ owners/.test(line)) || !packet.safetyContract?.some((line) => /15V674000/.test(line)) || !packet.safetyContract?.some((line) => /selects no PDFs/.test(line)) || !packet.safetyContract?.some((line) => /No production write/.test(line))) errors.push('safety contract incomplete'); const exactUrls = new Set(Object.values(OTHER_SOURCES).map((source) => source.url));
  for (const row of rows) { const source = expectedById.get(row.id); if (!source) { errors.push(`${row.id}: unknown row`); continue; } const frozen = fullRecord(source); if (!equal(row.before, frozen) || row.beforeSha256 !== hashValue(frozen)) errors.push(`${row.id}: before state drifted`); if (row.proposalSha256 !== hashValue(row.proposal) || !equal(row.changedFields, diffFields(row.before, row.proposal))) errors.push(`${row.id}: proposal hash/diff drifted`); for (const field of FULL_RECORD_FIELDS) if (!(field in row.before) || !(field in row.proposal)) errors.push(`${row.id}: missing field ${field}`); for (const field of Object.keys(row.proposal || {})) if (!FULL_RECORD_FIELDS.includes(field)) errors.push(`${row.id}: unauthorized proposal field ${field}`); for (const field of IMMUTABLE_FIELDS) if (!equal(row.before[field], row.proposal[field])) errors.push(`${row.id}: immutable ${field} changed`); for (const field of row.changedFields) if (!ALLOWED_CHANGED_FIELDS.has(field)) errors.push(`${row.id}: unauthorized changed field ${field}`); const expectedAction = retained.has(row.id) ? 'retain_indexed_identity_and_accuracy_cleanup' : 'hold_indexed_identity_and_accuracy_cleanup_pending_identity_policy'; if (row.action !== expectedAction || row.identityReviewRequired !== !retained.has(row.id) || row.identityConflict !== identityConflictFor(row.id)) errors.push(`${row.id}: identity verdict drifted`); if (row.proposal.status !== 'published') errors.push(`${row.id}: published status drifted`); if (fabricated.has(row.id)) { if (row.proposal.reportCount !== 0 || !(row.before.reportCount > 0)) errors.push(`${row.id}: fabricated report count must remain proposal-only zero`); } else if (row.proposal.reportCount !== row.before.reportCount) errors.push(`${row.id}: report count drifted`); if (/\b\d[\d,.]*\+\s*owners?\b|\bowners? have reported\b|\breported by \d[\d,.]* owners?\b/i.test(prose(row.proposal))) errors.push(`${row.id}: owner social proof is forbidden`); if (/youtube\.com|repairpal|clubprotege|carproblemzoo|carcomplaints|mazdaproblems|mazdaforum|autocodes|autoblog|nbcnews|edmunds|millsupply/i.test(JSON.stringify(row.proposal.citations))) errors.push(`${row.id}: secondary, commerce or fabricated citation survived`); if (row.proposal.humanApproved !== false || row.proposal.fixParts.length || row.proposal.communityRecommendations.length) errors.push(`${row.id}: must remain unapproved and commerce-free`); if (!/Do not buy/.test(row.proposal.solution) || !/No universal retail part/i.test(row.commerceDecision || '') || row.commerceDecision !== commerceDecisionFor(row.id)) errors.push(`${row.id}: commerce boundary missing`); if (!equal(row.proposal.citations, citationsFor(row.id)) || row.proposal.citations.length !== 1) errors.push(`${row.id}: exact citations drifted`); for (const citation of row.proposal.citations) if (!exactUrls.has(citation.url) || searchStyle(citation.url)) errors.push(`${row.id}: citation is not an approved primary source`); if (row.proposal.estimatedCostLow !== null || row.proposal.estimatedCostHigh !== null || row.proposal.typicalMileageLow !== null || row.proposal.typicalMileageHigh !== null || row.proposal.affectedSystems.length || row.proposal.dtcCodes.length) errors.push(`${row.id}: unsupported cost/mileage/system/code retained`); }
  const byId = new Map(rows.map((row) => [row.id, row.proposal])); const head = byId.get(IDS.headGasket); if (!head || !/194-row/.test(head.description) || !/do not by themselves identify/.test(head.description) || /Fel-Pro|coffee-with-too-much-creamer/i.test(prose(head))) errors.push('head-gasket boundary drifted'); const alternator = byId.get(IDS.alternator); if (!alternator || !/fabricated/.test(alternator.description) || !/80,000-120,000-mile/.test(alternator.description) || /DENSO|Bosch|13\.8-14\.4V/i.test(prose(alternator))) errors.push('alternator boundary drifted'); const transmission = byId.get(IDS.transmission); if (!transmission || !/6092410/.test(transmission.description) || !/pinched by a metal clip/.test(transmission.description) || !/10206002/.test(transmission.solution) || /CarComplaints ranks|Service the fluid and filter/i.test(prose(transmission))) errors.push('transmission boundary drifted'); const distributor = byId.get(IDS.distributor); if (!distributor || !/does not establish/.test(distributor.description) || /replace the complete distributor|fixed by a dealer distributor/i.test(prose(distributor))) errors.push('distributor boundary drifted'); const egr = byId.get(IDS.egr); if (!egr || !/54218/.test(egr.description) || !/1995-1996/.test(egr.description) || !/P0400/.test(egr.description) || !/P0401/.test(egr.description) || /carbon-clogged EGR valve.*classic cause/i.test(prose(egr))) errors.push('EGR boundary drifted'); const mount = byId.get(IDS.engineMount); if (!mount || !/fabricated/.test(mount.description) || !/40,000-80,000-mile/.test(mount.description) || /\$50-\$100|30-60 minutes/i.test(prose(mount))) errors.push('engine-mount boundary drifted'); const ignition = byId.get(IDS.ignitionRecall); if (!ignition || !/15V674000/.test(ignition.description) || !/8715J/.test(ignition.description) || !/become conductive and overheat/.test(ignition.description) || !/confirmed by VIN/.test(ignition.solution) || /parts supply eventually dried up/i.test(prose(ignition))) errors.push('ignition-recall boundary drifted'); const sway = byId.get(IDS.sway); if (!sway || !/10005202/.test(sway.description) || !/2001 MP3/.test(sway.description) || !/120-owner total/.test(sway.description) || /Consider polyurethane/i.test(prose(sway))) errors.push('sway boundary drifted'); const rust = byId.get(IDS.rust); if (!rust || !/does not establish a universal/.test(rust.description) || /factory primer.*did not fully reach|as early as ~40,000 miles/i.test(prose(rust))) errors.push('rust boundary drifted'); const thermostat = byId.get(IDS.thermostat); if (!thermostat || !/140-owner total/.test(thermostat.description) || !/does not establish/.test(thermostat.description) || /past 80k miles|Use OEM-temperature/i.test(prose(thermostat))) errors.push('thermostat boundary drifted'); if (!equal(packet.pdfSources, {}) || Object.keys(PDF_SOURCES).length !== 0) errors.push('Protege packet must select zero PDFs'); if (!equal(packet.otherSources, OTHER_SOURCES)) errors.push('other primary source metadata drifted'); return errors; }
function main() { const packet = JSON.parse(fs.readFileSync(OUTPUT, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const errors = validatePacket(packet, snapshot); console.log(JSON.stringify({ passed: errors.length === 0, packetSha256: hashValue(packet), decisionCount: packet.rows.length, applicationGate: packet.applicationGate, errors }, null, 2)); if (errors.length) process.exitCode = 1; } if (require.main === module) main(); module.exports = { ALLOWED_CHANGED_FIELDS, IMMUTABLE_FIELDS, validatePacket };
