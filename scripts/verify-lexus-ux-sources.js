/* eslint-disable @typescript-eslint/no-require-imports */
const crypto = require('node:crypto'); const fs = require('node:fs'); const { RECALL_FILES, SOURCE_FILES } = require('./lexus-adjudication-utils'); const { inspect } = require('./inspect-lexus-source-inventory'); const { BULLETIN_INVENTORY, CAMPAIGNS, MODEL_ALIASES, PDF_SOURCES, RECALL_INVENTORY } = require('./build-lexus-ux-adjudication'); function stable(value) { if (Array.isArray(value)) return value.map(stable); if (value && typeof value === 'object') return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stable(value[key])])); return value; } function equal(a, b) { return JSON.stringify(stable(a)) === JSON.stringify(stable(b)); } function hash(buffer) { return crypto.createHash('sha256').update(buffer).digest('hex'); }
async function verifyFiles(files) { const out = []; for (const source of files) { const buffer = await fs.promises.readFile(source.path); if (buffer.length !== source.length || hash(buffer) !== source.sha256) throw new Error(`${source.period}: source drift`); out.push({ period: source.period, bytes: buffer.length, sha256: hash(buffer) }); } return out; }
function verifyPdfBuffer(buffer, source, label) { const sha256 = hash(buffer); if (buffer.length !== source.bytes || sha256 !== source.sha256 || !buffer.subarray(0, 5).equals(Buffer.from('%PDF-'))) throw new Error(`${source.nhtsaDocumentId} ${label}: PDF/hash mismatch`); return { label, bytes: buffer.length, sha256 }; } async function verifyPdf(source) { const local = verifyPdfBuffer(fs.readFileSync(source.localPath), source, 'local'); const response = await fetch(source.url, { headers: { accept: 'application/pdf', 'user-agent': 'au7o-known-issue-source-audit/1.0' } }); if (!response.ok) throw new Error(`${source.nhtsaDocumentId}: status ${response.status}`); const remote = verifyPdfBuffer(Buffer.from(await response.arrayBuffer()), source, 'remote'); return { url: source.url, documentId: source.nhtsaDocumentId, pages: source.pages, contentType: response.headers.get('content-type') || '', local, remote }; }
async function verifyInventory() { const result = await inspect({ aliases: MODEL_ALIASES }); if (!equal(result.communicationCounts, BULLETIN_INVENTORY.periodCounts) || result.communicationTotal !== 229) throw new Error('UX communication drift'); if (!equal(result.recallCounts, RECALL_INVENTORY.periodCounts) || result.recallRows.length !== 2) throw new Error('UX recall drift'); const campaigns = [...new Set(result.recallRows.map((row) => row.campaign))].sort(); if (!equal(campaigns, CAMPAIGNS)) throw new Error('UX campaign drift'); const byId = new Map(result.relevantCommunications.map((row) => [row.id, row])); const drive = byId.get('10177590'); if (!drive || !/surge\/hesitation condition \(inconsistent acceleration\)/i.test(drive.summary) || !/^2019,2020,2021$/.test(drive.years) || drive.model !== 'UX200') throw new Error('UX drivability source boundary drift'); const media2019 = byId.get('10159733'); const media2022 = byId.get('10235224'); const media2023 = byId.get('10251079'); if (!media2019 || !/Display screen frozen/i.test(media2019.summary) || !/Apple CarPlay/i.test(media2019.summary)) throw new Error('UX 2019 multimedia boundary drift'); if (!media2022 || !/Poor map screen touch operation|Navigation system resets?/i.test(media2022.summary) || !/Android Auto/i.test(media2022.summary)) throw new Error('UX 2020-2022 multimedia boundary drift'); if (!media2023 || !/User interface|Bluetooth|Bug fixes/i.test(media2023.summary)) throw new Error('UX 2023 multimedia boundary drift'); return { communicationCounts: result.communicationCounts, communicationTotal: result.communicationTotal, recallCounts: result.recallCounts, recallTotal: result.recallRows.length, campaigns, exactDocumentIds: [drive.id, media2019.id, media2022.id, media2023.id], drivabilityBoundary: { model: drive.model, years: drive.years } }; }
async function main() { const [communicationFiles, recallFiles, inventory, pdfs] = await Promise.all([verifyFiles(SOURCE_FILES), verifyFiles(RECALL_FILES), verifyInventory(), Promise.all(Object.entries(PDF_SOURCES).map(async ([key, source]) => [key, await verifyPdf(source)]))]); console.log(JSON.stringify({ passed: true, communicationFiles, recallFiles, inventory, pdfs: Object.fromEntries(pdfs) }, null, 2)); } if (require.main === module) main().catch((error) => { console.error(error); process.exitCode = 1; });
