/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path'); const test = require('node:test'); const { EXPECTED_CAMPAIGNS, IDS, SOURCES } = require('./build-infiniti-qx60-adjudication'); const { validatePacket } = require('./validate-infiniti-qx60-adjudication');
const ROOT = path.resolve(__dirname, '..'); const packet = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', 'known-issue-infiniti-qx60-adjudication-2026-08-06.json'), 'utf8')); const snapshot = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'), 'utf8'));
test('QX60 hold packet passes the complete safety contract', () => assert.deepEqual(validatePacket(packet, snapshot, packet.source.snapshotSha256), []));
test('all seven QX60 rows remain byte-for-byte frozen', () => { assert.equal(packet.rows.length, 7); for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.equal(row.beforeSha256, row.proposalSha256, row.id); assert.deepEqual(row.changedFields, [], row.id); assert.deepEqual(row.proposal, row.before, row.id); } });
test('packet covers every frozen QX60 ID exactly once', () => { const expected = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'QX60').map((row) => row.id).sort(); const actual = packet.rows.map((row) => row.id).sort(); assert.deepEqual(actual, expected); assert.equal(new Set(actual).size, 7); });
test('partial CVT evidence and nonexistent camera recall cannot rewrite', () => { const cvt = packet.rows.find((row) => row.id === IDS.cvtFailure); assert.deepEqual(cvt.evidence.map((item) => item.url), [SOURCES.cvtRepair]); assert.match(packet.rows.find((row) => row.id === IDS.camera).reason, /24V321.*absent/i); assert.equal(Object.values(EXPECTED_CAMPAIGNS).flat().includes('24V321000'), false); });
