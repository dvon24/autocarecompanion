/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const test = require('node:test'); const assert = require('node:assert/strict'); const { clone } = require('./lexus-adjudication-utils'); const { IDS, SNAPSHOT, buildPacket } = require('./build-lexus-gs300-adjudication'); const { validatePacket } = require('./validate-lexus-gs300-adjudication'); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT,'utf8'));
test('GS300 packet passes the full two-page safety contract', () => { assert.deepEqual(validatePacket(buildPacket(snapshot),snapshot),[]); });
test('validator rejects indexed identity, engine and severity drift', () => { const packet = buildPacket(snapshot); packet.rows[0].proposal.title = 'Changed'; packet.rows[0].proposal.engines = ['Guess']; packet.rows[0].proposal.severity = 'critical'; const errors = validatePacket(packet,snapshot); assert.ok(errors.some((error) => /proposal drift|immutable title/.test(error))); assert.ok(errors.some((error) => /immutable engines/.test(error))); assert.ok(errors.some((error) => /severity/.test(error))); });
test('validator rejects archives and guessed commerce', () => { const packet = buildPacket(snapshot); packet.rows[1].proposal.status = 'archived'; packet.rows[1].proposal.fixParts = [{ oemPartNumber: 'GUESS' }]; assert.ok(validatePacket(packet,snapshot).some((error) => /proposal drift|publication\/source\/severity|derived-data\/commerce/.test(error))); });
test('validator rejects unsupported timing interval and bundled repair', () => { const packet = clone(buildPacket(snapshot)); const row = packet.rows.find((item) => item.id === IDS.timing); row.proposal.solution += ' Replace at 90,000 miles and replace the water pump at the same time.'; assert.ok(validatePacket(packet,snapshot).some((error) => /unsupported prescription/.test(error))); });
test('validator rejects wrong-generation bushing promotion', () => { const packet = clone(buildPacket(snapshot)); const row = packet.rows.find((item) => item.id === IDS.bushing); row.proposal.solution += ' Install Prothane 18-212.'; assert.ok(validatePacket(packet,snapshot).some((error) => /unsupported prescription/.test(error))); });
