/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path'); const test = require('node:test'); const { IDS, SOURCES } = require('./build-infiniti-qx80-adjudication'); const { validatePacket } = require('./validate-infiniti-qx80-adjudication');
const ROOT = path.resolve(__dirname, '..'); const packet = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', 'known-issue-infiniti-qx80-adjudication-2026-08-06.json'), 'utf8')); const snapshot = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'), 'utf8'));
test('QX80 hold packet passes the complete safety contract', () => assert.deepEqual(validatePacket(packet, snapshot, packet.source.snapshotSha256), []));
test('all three QX80 rows remain byte-for-byte frozen', () => { assert.equal(packet.rows.length, 3); for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.equal(row.beforeSha256, row.proposalSha256, row.id); assert.deepEqual(row.changedFields, [], row.id); assert.deepEqual(row.proposal, row.before, row.id); } });
test('packet covers every frozen QX80 ID exactly once', () => { const expected = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'QX80').map((row) => row.id).sort(); const actual = packet.rows.map((row) => row.id).sort(); assert.deepEqual(actual, expected); assert.equal(new Set(actual).size, 3); });
test('QX56 timing and partial infotainment evidence cannot rewrite', () => { assert.match(packet.rows.find((row) => row.id === IDS.timing).reason, /QX56/i); assert.deepEqual(packet.rows.find((row) => row.id === IDS.infotainment).evidence.map((item) => item.url), [SOURCES.diagnostic, SOURCES.vehicleUpdate]); });
