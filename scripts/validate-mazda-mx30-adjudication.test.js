/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const test = require('node:test'); const { IDS, OUTPUT, SNAPSHOT } = require('./build-mazda-mx30-adjudication'); const { hashValue } = require('./mazda-adjudication-utils'); const { validatePacket } = require('./validate-mazda-mx30-adjudication'); const packet = JSON.parse(fs.readFileSync(OUTPUT, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); function clone(value) { return JSON.parse(JSON.stringify(value)); } function row(target, id) { return target.rows.find((item) => item.id === id); } function rehash(item) { item.proposalSha256 = hashValue(item.proposal); } function rejects(name, mutate, expected) { test(name, () => { const changed = clone(packet); mutate(changed); const errors = validatePacket(changed, snapshot); assert.ok(errors.some((error) => error.includes(expected)), JSON.stringify(errors)); }); }
test('frozen MX-30 packet passes', () => assert.deepEqual(validatePacket(packet, snapshot), [])); rejects('rejects title change', (p) => { const item = row(p, IDS.battery); item.proposal.title += ' changed'; rehash(item); }, 'immutable title changed'); rejects('rejects year change', (p) => { const item = row(p, IDS.charging); item.proposal.years.pop(); rehash(item); }, 'immutable years changed'); rejects('rejects archive', (p) => { const item = row(p, IDS.range); item.proposal.status = 'archived'; rehash(item); }, 'immutable status changed'); rejects('rejects owner social proof', (p) => { const item = row(p, IDS.doorAccess); item.proposal.description += ' 0+ owners have reported this issue.'; rehash(item); }, 'owner social proof'); rejects('rejects commerce', (p) => { const item = row(p, IDS.doorHinge); item.proposal.fixParts = [{ name: 'hinge' }]; rehash(item); }, 'commerce-free'); rejects('rejects search citation', (p) => { const item = row(p, IDS.battery); item.proposal.citations[0].url += '?search=battery'; rehash(item); }, 'exact citations drifted'); rejects('rejects hold converted to retain', (p) => { row(p, IDS.battery).action = 'retain_indexed_identity_and_accuracy_cleanup'; }, 'identity hold drifted'); rejects('rejects blocker removal', (p) => { p.applicationGate.blockerRecordIds.pop(); }, 'blocker IDs'); rejects('rejects fabricated battery count', (p) => { const item = row(p, IDS.battery); item.proposal.reportCount = 80; rehash(item); }, 'fabricated report count'); rejects('rejects fabricated charging count', (p) => { const item = row(p, IDS.charging); item.proposal.reportCount = 90; rehash(item); }, 'fabricated report count'); rejects('rejects fabricated door count', (p) => { const item = row(p, IDS.doorAccess); item.proposal.reportCount = 45; rehash(item); }, 'fabricated report count'); rejects('rejects wrong campaign advice', (p) => { const item = row(p, IDS.battery); item.proposal.solution += ' Get the AM035A software update.'; rehash(item); }, 'battery boundary'); rejects('rejects invented charge-rate update', (p) => { const item = row(p, IDS.charging); item.proposal.solution += ' This raises the maximum charging rate.'; rehash(item); }, 'charging boundary'); rejects('rejects PDF injection', (p) => { p.pdfSources.fake = { url: 'https://example.com/fake.pdf' }; }, 'select zero PDFs'); rejects('rejects human approval', (p) => { const item = row(p, IDS.battery); item.proposal.humanApproved = true; rehash(item); }, 'unapproved'); rejects('rejects related-link mutation', (p) => { const item = row(p, IDS.range); item.proposal.relatedIssueIds = ['fake']; rehash(item); }, 'immutable relatedIssueIds changed');
