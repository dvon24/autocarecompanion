/* eslint-disable @typescript-eslint/no-require-imports */
const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path'); const test = require('node:test'); const { IDS, SOURCES } = require('./build-infiniti-qx70-adjudication'); const { validatePacket } = require('./validate-infiniti-qx70-adjudication');
const ROOT = path.resolve(__dirname, '..'); const packet = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', 'known-issue-infiniti-qx70-adjudication-2026-08-06.json'), 'utf8')); const snapshot = JSON.parse(fs.readFileSync(path.join(ROOT, 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'), 'utf8'));
test('QX70 hold packet passes the complete safety contract', () => assert.deepEqual(validatePacket(packet, snapshot, packet.source.snapshotSha256), []));
test('both indexed QX70 rows remain byte-for-byte frozen', () => { assert.equal(packet.rows.length, 2); for (const row of packet.rows) { assert.equal(row.action, 'keep_published_pending_source'); assert.equal(row.beforeSha256, row.proposalSha256, row.id); assert.deepEqual(row.changedFields, [], row.id); assert.deepEqual(row.proposal, row.before, row.id); } });
test('packet covers every frozen QX70 ID exactly once', () => { const expected = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'QX70').map((row) => row.id).sort(); const actual = packet.rows.map((row) => row.id).sort(); assert.deepEqual(actual, expected); assert.equal(new Set(actual).size, 2); });
test('V8 timing campaign and QX60 transfer leak are not transferred', () => { const timing = packet.rows.find((row) => row.id === IDS.timing); assert.equal(timing.evidence[0].url, SOURCES.timingCampaign); assert.match(timing.reason, /V8/i); assert.match(packet.rows.find((row) => row.id === IDS.transferLeak).reason, /QX60/i); });
