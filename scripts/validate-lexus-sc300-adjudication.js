/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const path = require('node:path');
const { FULL_RECORD_FIELDS, fullRecord, hashValue, normalizedFileHash, stableValue } = require('./lexus-adjudication-utils');
const { BLOCKER_IDS, BULLETIN_INVENTORY, IDS, RECALL_INVENTORY, REVIEW_DATE, SECONDARY_SOURCES, SNAPSHOT, buildPacket, citationsFor, contentFor, evidenceFor, proposalFor } = require('./build-lexus-sc300-adjudication');
const PACKET = path.resolve(__dirname, '..', 'data', 'known-issue-lexus-sc300-adjudication-2026-08-09.json');
const IMMUTABLE_FIELDS = Object.freeze(['make', 'model', 'years', 'trims', 'engines', 'category', 'title', 'severity', 'status', 'relatedIssueIds']);
const OBSERVATION_COVERAGE = Object.freeze({ 'sc300-transmission-misdiagnosis-corrected': [IDS.transmission], 'sc300-interchange-and-repair-bounds': [IDS.controlArm, IDS.window], 'sc300-leak-source-conflicts-exposed': [IDS.powerSteering, IDS.vvti], 'sc300-ignition-case-bounded': [IDS.distributor], 'sc300-no-unverified-commerce': BLOCKER_IDS, 'all-sc300-pages-preserved': BLOCKER_IDS });
const MARKERS = Object.freeze({
  [IDS.transmission]: 'no universal ATF, ECU, torque converter, valve body, solenoid, rebuild kit or retail part is asserted.', [IDS.distributor]: 'no universal cap, rotor, distributor, wire set, gasket, seal or retail part is asserted.', [IDS.controlArm]: 'no universal control arm, bushing, ball joint, Supra interchange or retail part is asserted.', [IDS.powerSteering]: 'no universal pump, seal kit, pressure valve, alternator or retail part is asserted.', [IDS.window]: 'no universal regulator, motor, carrier, sweep, adhesive repair or retail part is asserted.', [IDS.vvti]: 'no universal O-ring, cam gear, cam seal, timing belt, kit or retail part is asserted.',
});
const UNSAFE_PATTERNS = Object.freeze({
  [IDS.transmission]: /showing age around 130,000-150,000|worn torque-converter lockup clutch|valve body and shift solenoids degrade|Type T-IV|full A340E rebuild[^.!?]{0,40}(?:is|fix)/i,
  [IDS.distributor]: /with age the cap and rotor crack|valve-cover gasket[^.!?]{0,50}overlapping misfire|replace brittle plug wires as preventive|inspect\/clean the throttle body/i,
  [IDS.controlArm]: /Supra MkIV[^.!?]{0,50}interchange|later-gen Supra arms interchange|do both sides/i,
  [IDS.powerSteering]: /pump seals?[^.!?]{0,40}commonly fail|shared LS400|doing both the pump and alternator together is common/i,
  [IDS.window]: /clicking or POP sound[^.!?]{0,40}classic early warning|aggravated by dried outer window sweep|reinforce the nylon carrier with drilled holes/i,
  [IDS.vvti]: /most commonly reported oil leaks|Toyota advises against disassembling|aftermarket Viton|dealers typically only sell|replace the timing belt if due since access overlaps/i,
});
function equal(left, right) { return JSON.stringify(stableValue(left)) === JSON.stringify(stableValue(right)); }
function hasUnsafePositiveClaim(id, text) { return String(text || '').split(/(?<=[.!?])\s+/).some((sentence) => UNSAFE_PATTERNS[id]?.test(sentence) && !/(?:do not|does not|did not|not support|not establish|cannot|unverified|unsupported|remove|removed|no reviewed|rather than|without)/i.test(sentence)); }
function validatePacket(packet, snapshot, expectedSnapshotSha256 = normalizedFileHash(SNAPSHOT)) {
  const errors = []; const modelRows = snapshot.records.filter((row) => row.make === 'Lexus' && row.model === 'SC300').sort((a, b) => a.id.localeCompare(b.id)); const frozenById = new Map(modelRows.map((row) => [row.id, row])); const ids = packet.rows?.map((row) => row.id) || [];
  if (packet.status !== 'proposal-only' || packet.requiresIndependentApproval !== true || packet.auditStage !== 'model-primary-and-direct-source-adjudication') errors.push('packet safety status mismatch');
  if (packet.make !== 'Lexus' || packet.model !== 'SC300') errors.push('packet scope mismatch');
  if (packet.applicationGate?.status !== 'blocked' || !equal(packet.applicationGate?.blockerRecordIds, BLOCKER_IDS)) errors.push('application blocker mismatch');
  if (packet.source?.snapshotSha256 !== expectedSnapshotSha256 || packet.source?.snapshotHash !== snapshot.snapshotHash) errors.push('snapshot binding mismatch');
  if (packet.source?.modelRecordCount !== 6 || modelRows.length !== 6 || !equal(ids, BLOCKER_IDS)) errors.push('SC300 frozen-row coverage mismatch');
  if (!equal(packet.summary, { retain_indexed_identity_and_targeted_accuracy_cleanup_pending_source: 6, total: 6 })) errors.push('summary mismatch');
  if (!equal(packet.secondarySourceReview, SECONDARY_SOURCES)) errors.push('secondary-source review drift');
  for (const source of Object.values(packet.secondarySourceReview || {})) { if (!/^https:\/\//.test(source.url || '') || /google\.[^/]+\/search|bing\.com\/search|duckduckgo\.com/i.test(source.url || '')) errors.push(`source is not direct: ${source.url || '<missing>'}`); if (source.liveAccess !== 'protected-403-direct-url-reviewed' || !source.assertedBoundary) errors.push(`source boundary missing: ${source.url || '<missing>'}`); }
  if (!equal(packet.manufacturerCommunications, BULLETIN_INVENTORY) || BULLETIN_INVENTORY.totalRows !== 57) errors.push('communication inventory mismatch');
  if (!equal(packet.recallInventory, RECALL_INVENTORY) || RECALL_INVENTORY.totalRows !== 0 || RECALL_INVENTORY.campaignCount !== 0) errors.push('recall inventory mismatch');
  for (const row of packet.rows || []) { const frozen = frozenById.get(row.id); if (!frozen) { errors.push(`${row.id}: not in frozen SC300 scope`); continue; } const before = fullRecord(frozen); const expectedProposal = proposalFor(frozen);
    if (row.action !== 'retain_indexed_identity_and_targeted_accuracy_cleanup_pending_source' || row.commerceDecision !== 'diagnosis-and-fitment-specific-no-universal-retail-part') errors.push(`${row.id}: decision mismatch`);
    if (!equal(row.before, before) || row.beforeSha256 !== hashValue(before)) errors.push(`${row.id}: before drift`); if (!equal(row.proposal, expectedProposal) || row.proposalSha256 !== hashValue(expectedProposal)) errors.push(`${row.id}: proposal drift`);
    const changedFields = FULL_RECORD_FIELDS.filter((field) => hashValue(before[field]) !== hashValue(expectedProposal[field])); if (!equal(row.changedFields, changedFields) || !row.changedFields.length) errors.push(`${row.id}: changed-field drift`); if (!equal(row.evidence, evidenceFor(frozen)) || row.evidence.length !== 4) errors.push(`${row.id}: evidence drift`);
    for (const field of IMMUTABLE_FIELDS) if (!equal(row.proposal[field], before[field])) errors.push(`${row.id}: immutable ${field} drift`); for (const field of FULL_RECORD_FIELDS) if (!Object.prototype.hasOwnProperty.call(row.before, field) || !Object.prototype.hasOwnProperty.call(row.proposal, field)) errors.push(`${row.id}: missing ${field}`);
    if (!['high', 'medium', 'low'].includes(row.proposal.severity) || row.proposal.status !== 'published' || /^Archived\s*-/i.test(row.proposal.title) || row.proposal.humanApproved !== false || row.proposal.reportCount !== 0 || row.proposal.source !== 'manual' || row.proposal.confidence !== contentFor(row.id).confidence) errors.push(`${row.id}: publication/source/severity/confidence drift`);
    if (row.proposal.reviewedOn !== REVIEW_DATE || row.proposal.contentUpdatedOn !== REVIEW_DATE || !row.proposal.contentUpdateSummary) errors.push(`${row.id}: review metadata drift`);
    if (!equal(row.proposal.symptoms, []) || !equal(row.proposal.affectedSystems, []) || !equal(row.proposal.dtcCodes, []) || !equal(row.proposal.communityRecommendations, []) || !equal(row.proposal.fixParts, [])) errors.push(`${row.id}: derived-data/commerce drift`);
    if ([row.proposal.estimatedCostLow, row.proposal.estimatedCostHigh, row.proposal.typicalMileageLow, row.proposal.typicalMileageHigh].some((value) => value !== null)) errors.push(`${row.id}: cost/mileage remains`); if (!equal(row.proposal.citations, citationsFor(row.id))) errors.push(`${row.id}: citation boundary mismatch`); if (!String(row.proposal.solution).includes(MARKERS[row.id])) errors.push(`${row.id}: explicit no-commerce marker missing`); if (hasUnsafePositiveClaim(row.id, `${row.proposal.description} ${row.proposal.solution}`)) errors.push(`${row.id}: unsupported positive claim survived`); if (/amazon\.com\/s\?k=|ebay\.com\/sch|rockauto\.com\/en\/partsearch|google\.[^/]+\/search|bing\.com\/search/i.test(JSON.stringify(row.proposal))) errors.push(`${row.id}: search-style commerce/source survived`); if (/\b0\+ owners have reported this issue\b/i.test(JSON.stringify(row.proposal))) errors.push(`${row.id}: zero-owner social proof survived`);
  }
  const transmission = packet.rows?.find((row) => row.id === IDS.transmission)?.proposal; if (!/ECU replacement restored overdrive/i.test(transmission?.description || '') || !/Do not prescribe a converter, valve body, solenoids or rebuild/i.test(transmission?.solution || '')) errors.push('SC300 transmission correction missing');
  const controlArm = packet.rows?.find((row) => row.id === IDS.controlArm)?.proposal; if (!/one 1999 SC300/i.test(controlArm?.description || '') || !/do not generalize/i.test(controlArm?.solution || '')) errors.push('SC300 fitment boundary missing');
  const powerSteering = packet.rows?.find((row) => row.id === IDS.powerSteering)?.proposal; if (!/disputed applying the SC400 pump-over-alternator layout/i.test(powerSteering?.description || '') || !/Identify the fluid/i.test(powerSteering?.solution || '')) errors.push('SC300 leak-path correction missing');
  for (const [code, expectedIds] of Object.entries(OBSERVATION_COVERAGE)) { const observation = packet.observations?.find((item) => item.code === code); if (!observation) errors.push(`missing observation ${code}`); else if (!equal(observation.recordIds, expectedIds)) errors.push(`${code}: record coverage mismatch`); }
  if (!equal(packet, buildPacket(snapshot))) errors.push('packet differs from deterministic rebuild'); return errors;
}
if (require.main === module) { const packet = JSON.parse(fs.readFileSync(PACKET, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const errors = validatePacket(packet, snapshot); console.log(JSON.stringify({ passed: errors.length === 0, packetSha256: normalizedFileHash(PACKET), decisionCount: packet.rows?.length || 0, applicationGate: packet.applicationGate, errors }, null, 2)); if (errors.length) process.exitCode = 1; }
module.exports = { MARKERS, OBSERVATION_COVERAGE, PACKET, SNAPSHOT, UNSAFE_PATTERNS, hasUnsafePositiveClaim, validatePacket };
