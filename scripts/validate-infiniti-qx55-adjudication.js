/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const path = require('node:path'); const { FULL_RECORD_FIELDS, diffFields, fullRecord, hashValue, normalizedFileHash, stableValue } = require('./infiniti-adjudication-utils'); const { CARDS, EXPECTED_CAMPAIGNS, KEEP_REASONS, RECALL_QUERIES, SOURCES, evidenceFor, rewriteRecord } = require('./build-infiniti-qx55-adjudication');
const PACKET = path.resolve(__dirname, '..', 'data', 'known-issue-infiniti-qx55-adjudication-2026-08-06.json'); const SNAPSHOT = path.resolve(__dirname, '..', 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'); function equal(left, right) { return JSON.stringify(stableValue(left)) === JSON.stringify(stableValue(right)); }
function validatePacket(packet, snapshot, expectedSnapshotSha256 = normalizedFileHash(SNAPSHOT)) {
  const errors = []; const modelRows = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'QX55'); const frozenById = new Map(modelRows.map((row) => [row.id, row])); if (packet.status !== 'proposal-only' || packet.requiresIndependentApproval !== true) errors.push('packet safety status mismatch'); if (packet.make !== 'Infiniti' || packet.model !== 'QX55') errors.push('packet scope mismatch'); if (packet.source?.snapshotSha256 !== expectedSnapshotSha256 || packet.source?.snapshotHash !== snapshot.snapshotHash) errors.push('snapshot binding mismatch'); if (packet.source?.qx55RecordCount !== 8 || modelRows.length !== 8 || packet.rows?.length !== 8) errors.push('QX55 row count mismatch');
  const ids = packet.rows?.map((row) => row.id) || []; if (new Set(ids).size !== 8) errors.push('duplicate or missing IDs'); for (const id of frozenById.keys()) if (!ids.includes(id)) errors.push(`missing QX55 ID: ${id}`);
  for (const row of packet.rows || []) { const frozen = frozenById.get(row.id); if (!frozen) { errors.push(`unknown QX55 ID: ${row.id}`); continue; } const before = fullRecord(frozen); const isRewrite = Boolean(CARDS[row.id]); const expected = isRewrite ? rewriteRecord(before, row.id) : before; if (row.action !== (isRewrite ? 'rewrite_same_identity' : 'keep_published_pending_source')) errors.push(`${row.id}: action mismatch`); if (!isRewrite && row.reason !== KEEP_REASONS[row.id]) errors.push(`${row.id}: reason mismatch`); if (!equal(row.before, before) || !equal(row.proposal, expected)) errors.push(`${row.id}: proposal drift`); if (row.beforeSha256 !== hashValue(before) || row.proposalSha256 !== hashValue(expected) || !equal(row.changedFields, diffFields(before, expected))) errors.push(`${row.id}: hash/change mismatch`); if (row.proposal.make !== 'Infiniti' || row.proposal.model !== 'QX55' || row.proposal.title !== before.title || row.proposal.category !== before.category || !equal(row.proposal.years, before.years) || row.proposal.status !== 'published' || /^Archived\s*-/i.test(row.proposal.title)) errors.push(`${row.id}: identity/status drift`); if (!equal(row.evidence, evidenceFor(row.id))) errors.push(`${row.id}: evidence drift`); for (const field of FULL_RECORD_FIELDS) if (!Object.prototype.hasOwnProperty.call(row.before, field) || !Object.prototype.hasOwnProperty.call(row.proposal, field)) errors.push(`${row.id}: missing ${field}`); if (!isRewrite && !equal(row.proposal, row.before)) errors.push(`${row.id}: hold changed`); if (isRewrite) { for (const field of ['trims', 'engines', 'dtcCodes', 'communityRecommendations', 'fixParts']) if (row.proposal[field]?.length !== 0) errors.push(`${row.id}: ${field} must be empty`); for (const field of ['estimatedCostLow', 'estimatedCostHigh', 'typicalMileageLow', 'typicalMileageHigh']) if (row.proposal[field] !== null) errors.push(`${row.id}: ${field} must be null`); if (row.proposal.humanApproved !== false || row.proposal.reportCount !== 0 || row.proposal.source !== 'manual') errors.push(`${row.id}: approval/source contract mismatch`); } }
  if (packet.summary?.rewrite_same_identity !== 2 || packet.summary?.keep_published_pending_source !== 6 || packet.summary?.total !== 8) errors.push('summary mismatch'); if (!equal(packet.primarySources, { fan: SOURCES.fan, doorLock: SOURCES.doorLock })) errors.push('primary source map mismatch'); if (!equal(packet.mismatchSources, { bearing: SOURCES.bearing, aeb: SOURCES.aeb, diagnostic: SOURCES.diagnostic, recallQueries: RECALL_QUERIES, expectedCampaigns: EXPECTED_CAMPAIGNS })) errors.push('mismatch source map mismatch'); for (const code of ['qx55-two-exact-source-rewrites', 'qx55-bearing-scope-frozen', 'qx55-aeb-outcome-mismatch-frozen', 'all-qx55-pages-preserved']) if (!packet.observations?.some((item) => item.code === code)) errors.push(`missing observation ${code}`); return errors;
}
if (require.main === module) { const packet = JSON.parse(fs.readFileSync(PACKET, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const errors = validatePacket(packet, snapshot); console.log(JSON.stringify({ passed: errors.length === 0, packetSha256: normalizedFileHash(PACKET), decisionCount: packet.rows?.length || 0, errors }, null, 2)); if (errors.length) process.exitCode = 1; } module.exports = { validatePacket };
