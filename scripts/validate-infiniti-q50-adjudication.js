/* eslint-disable @typescript-eslint/no-require-imports */
const fs = require('node:fs'); const path = require('node:path'); const { FULL_RECORD_FIELDS, fullRecord, hashValue, normalizedFileHash, stableValue } = require('./infiniti-adjudication-utils'); const { EXPECTED_CAMPAIGNS, ID, KEEP_REASON, RECALL_QUERIES, SOURCES, evidence } = require('./build-infiniti-q50-adjudication');
const PACKET = path.resolve(__dirname, '..', 'data', 'known-issue-infiniti-q50-adjudication-2026-08-06.json'); const SNAPSHOT = path.resolve(__dirname, '..', 'data', '_infiniti-deeplink-snapshot-2026-08-06.json'); function equal(left, right) { return JSON.stringify(stableValue(left)) === JSON.stringify(stableValue(right)); }
function validatePacket(packet, snapshot, expectedSnapshotSha256 = normalizedFileHash(SNAPSHOT)) {
  const errors = []; const modelRows = snapshot.records.filter((row) => row.make === 'Infiniti' && row.model === 'Q50'); const frozen = modelRows[0]; const row = packet.rows?.[0];
  if (packet.status !== 'proposal-only' || packet.requiresIndependentApproval !== true) errors.push('packet safety status mismatch'); if (packet.make !== 'Infiniti' || packet.model !== 'Q50') errors.push('packet scope mismatch'); if (packet.source?.snapshotSha256 !== expectedSnapshotSha256 || packet.source?.snapshotHash !== snapshot.snapshotHash) errors.push('snapshot binding mismatch'); if (packet.source?.q50RecordCount !== 1 || modelRows.length !== 1 || packet.rows?.length !== 1 || row?.id !== ID || frozen?.id !== ID) errors.push('Q50 row count/identity mismatch');
  if (frozen && row) { const before = fullRecord(frozen); if (row.action !== 'keep_published_pending_source' || row.reason !== KEEP_REASON) errors.push('action/reason mismatch'); if (!equal(row.before, before) || !equal(row.proposal, before)) errors.push('frozen content changed'); if (row.beforeSha256 !== hashValue(before) || row.proposalSha256 !== row.beforeSha256 || row.changedFields?.length !== 0) errors.push('hash/change mismatch'); if (row.proposal.make !== 'Infiniti' || row.proposal.model !== 'Q50' || row.proposal.status !== 'published' || /^Archived\s*-/i.test(row.proposal.title)) errors.push('identity/status drift'); if (!equal(row.evidence, evidence())) errors.push('evidence drift'); for (const field of FULL_RECORD_FIELDS) if (!Object.prototype.hasOwnProperty.call(row.before, field) || !Object.prototype.hasOwnProperty.call(row.proposal, field)) errors.push(`missing ${field}`); }
  if (packet.summary?.rewrite_same_identity !== 0 || packet.summary?.keep_published_pending_source !== 1 || packet.summary?.total !== 1) errors.push('summary mismatch'); if (!equal(packet.reviewSources, SOURCES) || !equal(packet.mismatchSources, { recallQueries: RECALL_QUERIES, expectedCampaigns: EXPECTED_CAMPAIGNS })) errors.push('source map mismatch'); for (const code of ['intouch-evidence-partial-scope', 'q50-recall-inventory-deferred', 'q50-page-preserved']) if (!packet.observations?.some((item) => item.code === code)) errors.push(`missing observation ${code}`); return errors;
}
if (require.main === module) { const packet = JSON.parse(fs.readFileSync(PACKET, 'utf8')); const snapshot = JSON.parse(fs.readFileSync(SNAPSHOT, 'utf8')); const errors = validatePacket(packet, snapshot); console.log(JSON.stringify({ passed: errors.length === 0, packetSha256: normalizedFileHash(PACKET), decisionCount: packet.rows?.length || 0, errors }, null, 2)); if (errors.length) process.exitCode = 1; } module.exports = { validatePacket };
