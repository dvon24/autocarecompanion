/* eslint-disable @typescript-eslint/no-require-imports */
const { inspect } = require('./inspect-nhtsa-model-source-inventory');
const { getContract } = require('./peugeot-model-adjudication-contracts');
function argValue(flag) { const index = process.argv.indexOf(flag); return index >= 0 ? process.argv[index + 1] : ''; }
async function analyze(contract) { const inventory = await inspect({ makes: contract.sourceMakes || [contract.make], aliases: contract.modelAliases, terms: contract.searchTerms }); const ids = [...new Set(inventory.relevantCommunications.map((row) => row.id))].sort(); const campaigns = [...new Set(inventory.recallRows.map((row) => row.campaign))].sort(); const passed = inventory.communicationTotal === contract.bulletinInventory.totalRows && inventory.relevantCommunications.length === contract.bulletinInventory.relevantRowCount && ids.join('|') === contract.relevantDocumentIds.join('|') && inventory.recallCounts.pre === contract.recallInventory.periodCounts.pre && inventory.recallCounts.post === contract.recallInventory.periodCounts.post && inventory.recallRows.length === contract.recallInventory.totalRows && campaigns.join('|') === contract.campaigns.join('|'); return { passed, communicationCounts: inventory.communicationCounts, communicationTotal: inventory.communicationTotal, relevantCommunicationRows: inventory.relevantCommunications.length, uniqueRelevantCommunications: ids.length, relevantDocumentIds: ids, recallCounts: inventory.recallCounts, recallRows: inventory.recallRows.length, campaigns }; }
if (require.main === module) { const model = argValue('--model'); if (!model) throw new Error('--model is required'); analyze(getContract(model)).then((result) => { console.log(JSON.stringify(process.argv.includes('--compact') ? { ...result, relevantDocumentIds: undefined, campaigns: undefined, campaignCount: result.campaigns.length } : result, null, 2)); if (!result.passed) process.exitCode = 1; }).catch((error) => { console.error(error); process.exitCode = 1; }); }
module.exports = { analyze };
