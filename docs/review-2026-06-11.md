# Deep review — 2026-06-11

Multi-agent review (7 code dimensions + DB integrity + coverage gaps + photo-pipeline inventory), every code finding adversarially verified by a second agent reading the cited code.

**Run status:** dimensions api-security, payments, ai-flows completed + verified. Dimensions seo, data-layer, mobile-pwa, perf-build DIED on the Claude session limit and re-run later (resume wf_b2188407-379). The 14 findings under "Unverified" below had their verifier die — they are NOT refuted, just unconfirmed.

## Confirmed findings (verifier re-read the code and confirmed)

### [high] Video-mode vision exceeds Vercel maxDuration — function killed mid-call and the consumed photo credit is never refunded
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/vision/route.ts:20 (ai-flows)
- **Detail:** maxDuration is 60s, but the route's sequential upstream timeout budget is larger: Whisper transcription gets AbortSignal.timeout(25_000) (line 266) and the main vision call gets AbortSignal.timeout(55_000) (line 496), plus auth, two Prisma queries, and multipart parsing. A slow Whisper + slow vision call = up to ~80s of allowed upstream time. Vercel kills the function at 60s, the user gets an opaque platform 504 (no x-au7o-req-id, no JSON body the client gate cards understand), and — critically — failWithRefund never runs, violating the route's own invariant (lines 129-131: 'every failure path below must refundPhotoQuota'). Free users (3/month) and anons (1/month) permanently lose a credit to a timeout they can't control. The photo path can also graze the limit (55s call + overhead).
- **Fix:** Make the timeout budget fit the function: in video mode reduce the vision-call timeout to ~25s when Whisper already ran (or skip Whisper if more than ~20s has elapsed), or raise maxDuration to 90-120 for this route. Compute remaining = (maxDuration*1000 - 5000) - (Date.now() - t0) and pass that to AbortSignal.timeout for the vision fetch so the route always responds (and refunds) itself instead of being killed.
- **Verifier:** CONFIRMED. All cited code verified in src/app/api/vision/route.ts: maxDuration=60 (line 20, no vercel.json functions override), Whisper fetch with AbortSignal.timeout(25_000) at line 266, vision fetch with AbortSignal.timeout(55_000) at line 496, both sequential in video mode. checkAndConsumePhotoQuota durably consumes a credit in the DB (Prisma $transaction in src/lib/photo-quota.ts) before upstream work, and the only refund mechanism is failWithRefund (line 170), which cannot run if Vercel kills the function at 60s. The unguarded window is real: each upstream call can stay within its own tim

### [high] /api/chat consumes the weekly chat credit before parsing/validating the request and never refunds it on any failure path
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/chat/route.ts:882 (ai-flows)
- **Detail:** checkAndConsumeChatQuota runs at line 882, but the body isn't parsed until line 928 and the budget gate runs at line 917. So a 400 invalid-request, a 429 budgetExceeded, an OpenAI 500, the tool-call fallback failing, or a timeout all burn one of the free user's 5 weekly chats with no answer delivered. Unlike the vision route (which has refundPhotoQuota on every failure path), chat-quota.ts has no refund function at all — grep confirms no refund/decrement anywhere in src/lib/chat-quota.ts. For a user limited to 5 chats/week whose upsell depends on those chats demonstrating value, every transient OpenAI error silently eats a credit. The same consume-then-fail-without-refund pattern exists in /api/hub-chat (route.ts line 366: quota consumed, then the SSE stream can fail with 'error' and no refund).
- **Fix:** Reorder: parse body + ChatRequestSchema.safeParse + isBudgetExceeded BEFORE checkAndConsumeChatQuota. Add refundChatQuota(key) to src/lib/chat-quota.ts (mirror refundPhotoQuota's updateMany decrement) and call it in the catch block of /api/chat and in hub-chat's stream catch when assistantText is empty.
- **Verifier:** Verified in code. /api/chat/route.ts consumes the weekly quota at line 882 (checkAndConsumeChatQuota does a DB increment, chat-quota.ts lines 156-173), then: the isBudgetExceeded 429 gate runs at line 917, body parse + ChatRequestSchema 400 validation at lines 928-939, and OpenAI primary+fallback failure rethrows to the outer catch (lines 1070-1084, returning 504/500) — all after consumption, with no refund on any path. src/lib/chat-quota.ts exports only checkAndConsumeChatQuota and peekChatQuota; a repo-wide grep for refund confirms no refund/decrement exists for chat quota anywhere. The visi

### [high] /api/chat tool-calling worst case is ~3 sequential 60s OpenAI calls inside a 60s function — guaranteed platform 504 with no JSON error
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/chat/route.ts:46 (ai-flows)
- **Detail:** TIMEOUT_MS = 60000 exactly equals maxDuration = 60 (line 31), leaving zero buffer even for a single call (the guide route correctly uses 55s for this reason, line 213 of guide/route.ts). Worse, callOpenAI runs up to 2 tool rounds, and EACH follow-up fetch gets a fresh AbortSignal.timeout(TIMEOUT_MS) (line 552), plus DB-backed tool execution (lookup_known_issues, NHTSA fetch with its own 8s timeout) between rounds. Worst case is ~180s of allowed time in a 60s function. When the first call alone takes >55s the function is killed by Vercel before the route's own AbortError handler (line 1073) can return its friendly 504 JSON — the client gets an opaque FUNCTION_INVOCATION_TIMEOUT, and per the previous finding the weekly credit is gone.
- **Fix:** Use a single shared deadline: const deadline = Date.now() + 50_000; give every fetch AbortSignal.timeout(Math.max(1000, deadline - Date.now())) and skip remaining tool rounds when fewer than ~10s remain, returning the best content so far instead of dying.
- **Verifier:** Verified in src/app/api/chat/route.ts: TIMEOUT_MS=60000 (line 46) exactly equals maxDuration=60 (line 31), while guide/route.ts line 213 correctly uses 55000 with an explicit 'leave 5s buffer for Vercel' comment — proving the buffer omission is an oversight. callOpenAI's tool loop (lines 516-559) runs up to 2 follow-up fetches, each with a FRESH AbortSignal.timeout(60000) (line 552), plus inter-round tool execution (Prisma queries; NHTSA fetch with 8s timeout, line 437). The error-path fallback fetch (line 1007) adds yet another fresh 60s, so worst-case allowed wall time is ~250s inside a 60s 

### [high] The $25/month hard budget cap is non-functional in production — cost log lives in per-instance /tmp that resets on every cold start
- **Where:** C:/Users/devon/autocarecompanion/src/lib/costs.ts:65 (ai-flows)
- **Detail:** getCostsFilePath() uses /tmp on Vercel (line 65). /tmp is per-lambda-instance and ephemeral: every cold start begins with an empty file, and concurrent instances each have their own file. isBudgetExceeded() — the gate called by /api/chat, /api/guide, /api/guide/help, and /api/garage/assistant before every AI call — therefore almost always sees ~$0 and the 'Story 7.3 Hard Budget Cap' never fires in production. This is the only sitewide cost backstop; all four routes believe it exists. Additionally the pricing constants are GPT-4-era ($0.03/1K input, $0.06/1K output, lines 15-16) applied to every model, so even within one warm instance the numbers are wrong (the guide route separately estimates $10/$30 per 1M at guide/route.ts line 649 — two conflicting price tables).
- **Fix:** Persist costs in Postgres: a single monthly-aggregate row (e.g., ApiCostMonth { month, feature, costMicros }) incremented in logApiCost, with isBudgetExceeded() reading the current month's sum (cache it in-memory for 60s to avoid a query per request). Move the per-1K price constants into a per-model map.
- **Verifier:** Verified in code. costs.ts:64-65 routes the cost log to /tmp/api-costs.jsonl when VERCEL=1; the project deploys to Vercel (vercel.json present, and the /tmp fallback was itself added as a Vercel read-only-FS fix). Vercel /tmp is per-instance and reset on cold start, so getMonthlyEntries() only ever sees appends from the current warm instance — effectively ~$0 — and isBudgetExceeded() (costs.ts:215) never trips. All four cited routes gate on it exactly as claimed: chat/route.ts:917, guide/route.ts:579, guide/help/route.ts:143, garage/assistant/route.ts:400, each returning a 429 'budget exceeded

### [high] /api/guide: unauthenticated AI generation guarded only by a per-instance in-memory limiter, and the cache is trivially bypassed via the free-text trim field
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/guide/route.ts:563 (ai-flows)
- **Detail:** The route has no auth and no DB-backed quota — only guideLimiter (10/min/IP) which is an in-memory Map (rate-limit.ts line 22) that resets on cold start and exists separately in every concurrent lambda instance, so the effective limit under scale-out is 10/min × instances, and bots with rotating IPs ignore it entirely. The cache cannot save you: buildCacheKey includes the raw client-supplied trim (guide-cache.ts line 34), and GuideGenerationRequestSchema accepts any string — sending trim='x1', 'x2', ... forces a fresh ~$0.21 AI generation every request AND pollutes the CachedGuide table with garbage rows. With the budget cap broken (previous finding), there is no functioning ceiling on this spend.
- **Fix:** Require auth (or at minimum the same checkAndConsumeChatQuota-style DB quota used by chat) for cache-miss generation, and validate trim against the YMMT dataset (public/data/ymmt.json) — fall back to trim='' for unrecognized values so the cache key space is finite.
- **Verifier:** Verified against the cited code. (1) /api/guide POST (route.ts:562) has NO auth and NO DB-backed quota; its only per-request abuse guards are guideLimiter.check(ip), checkAiGate(), and isBudgetExceeded(). middleware.ts matches only /known-issues/:path*, so no auth wrapper covers this route. (2) checkAiGate (ai-gate.ts:34-37) is a GDPR opt-out that explicitly ALLOWS anonymous users through — it is not a quota and does nothing for unauthenticated abuse. (3) guideLimiter is a per-instance in-memory Map (rate-limit.ts:22), keyed solely on x-forwarded-for IP (getClientIp), and the file header itsel

### [medium] Unauthenticated admin/data endpoint is designed to dump collected emails + feedback PII
- **Where:** src/app/api/admin/data/route.ts:12 (api-security)
- **Detail:** GET reads data/interest-emails.txt and data/feedback.jsonl and returns every captured email + feedback message with no auth. On Vercel the project data dir is read-only/local-only (gitignored), so in the current prod deploy these files are typically absent and the endpoint returns empty arrays — which is the only thing limiting the exposure. The pattern is a latent PII leak: the moment collection is repointed to a writable/persisted path (or run anywhere the files exist), this anonymously serves the full interest-list and feedback PII.
- **Fix:** Add the admin guard (same requireAdmin helper) before reading the files. Don't ship an unauthenticated handler whose purpose is to return collected emails.
- **Verifier:** Verified in src/app/api/admin/data/route.ts: the GET handler has no authentication and is purpose-built to return all collected interest emails (data/interest-emails.txt) and feedback entries including emails/messages (data/feedback.jsonl). No protection exists elsewhere: src/middleware.ts only matches /known-issues/:path* and never runs on /api/admin; vercel.json has no route auth; grep for requireAdmin/ADMIN_KEY/ADMIN_TOKEN/ADMIN_SECRET across src/ returns zero hits (so the proposed fix's reference to an existing requireAdmin helper is slightly inaccurate — it must be created — but that does

### [medium] hub-chat: only the final message is length-checked — prior messages in the client-supplied array are forwarded to OpenAI with unbounded size and unvalidated roles
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/hub-chat/route.ts:307 (ai-flows)
- **Detail:** The MAX_INPUT_TOKENS (2000) guardrail at line 307 only inspects messages[messages.length-1]. On a new session (no sessionId), sessionMessages = messages.slice(-20) (line 426) takes up to 19 earlier client-supplied messages with NO per-message length check and NO runtime role validation (HubMessage is a TS type only; body.messages items are never checked against 'user'|'assistant'). All of them are sent verbatim to gpt-5.5 (lines 500-506, 532-534) and persisted to ChatSession. Two concrete abuses: (1) cost — 19 messages of several hundred KB each pass the gate and produce six-figure-token prompts; an authed free account gets 200/day of these (hubChatAuthedDayLimiter), so a single account can run up real OpenAI spend; (2) a message with role:'system' is forwarded as a system message, defeating the <user_message> injection guardrail and the topic-scope rules — turning the endpoint into a general-purpose LLM proxy.
- **Fix:** In the step-1 validation loop, reject any message whose role is not 'user'/'assistant', cap each message at ~8000 chars, and enforce estimateTokens(sum of all messages) <= ~6000 before building wrappedMessages. Apply the same caps when re-reading prevMessages from DB.
- **Verifier:** Verified in src/app/api/hub-chat/route.ts. The code-level claims are all accurate: (1) the MAX_INPUT_TOKENS=2000 guardrail at line 307 checks only messages[messages.length-1] (userMessage, line 301); (2) the only role check is line 298 (last message must be 'user') — earlier messages get no per-message length cap and no runtime role validation (HubMessage is a TS-only type); (3) on a new session with no sessionId, sessionMessages = messages.slice(-20) (line 426) is fully client-controlled, persisted (lines 440-450), and sent to OpenAI; (4) lines 500-506 forward every non-final message verbatim

### [medium] Guide generation has the exact reasoning-token empty-content trap the vision route already diagnosed and fixed — JSON.parse('') 500s
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/guide/route.ts:323 (ai-flows)
- **Detail:** callOpenAI uses gpt-5.4 with max_completion_tokens: 3000. On gpt-5.x, internal reasoning tokens count against this budget — the vision route's own comment (vision/route.ts lines 440-443) records that 1800 was 'fully consumed by reasoning, leaving message.content empty'. A complete guide JSON is ~1500-2500 output tokens, so 3000 leaves little reasoning headroom; whenever reasoning runs long, content comes back '' and parseGuideResponse does a bare JSON.parse(content) (line 360) which throws SyntaxError → 500 'Failed to parse guide data'. There is no empty-content check, no finish_reason check, and no retry. Same class of failure if the JSON is truncated at the token limit (response_format json_object does not guarantee completeness on length cutoff).
- **Fix:** Raise max_completion_tokens to ~6000 (mirroring vision's fix), and before parsing check `if (!content || data.choices[0]?.finish_reason === 'length')` → return a 502 with a retry message (and log finish_reason). Optionally one automatic retry on empty content.
- **Verifier:** Verified in src/app/api/guide/route.ts: callOpenAI (line 311-328) uses gpt-5.4 on Chat Completions with max_completion_tokens: 3000; line 338 defaults missing message.content to ''; parseGuideResponse line 360 does a bare JSON.parse(content) with no empty-content check, no finish_reason check, and no retry — JSON.parse('') throws SyntaxError which the catch at lines 692-697 maps to a 500 'Failed to parse guide data'. The codebase's own vision route (src/app/api/vision/route.ts lines 440-443) documents this exact failure on gpt-5.x: 'gpt-5.x burns 1-3k thinking' and a 1800-token budget 'was get

### [medium] A degenerate model response (e.g. steps: []) is cached permanently — first-writer-wins cache with zero output validation poisons the YMMT+type forever
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/guide/route.ts:671 (ai-flows)
- **Detail:** parseGuideResponse defaults every field (parsed.steps || [], title fallback, etc.) and nothing validates that the guide is usable before storeCachedGuide runs. storeCachedGuide upserts with `update: {}` (guide-cache.ts lines 164-166, 'first-writer wins'), so a single bad generation — valid JSON but empty steps, empty tools, or a refusal wrapped in JSON — becomes the permanent cached guide for that year/make/model/trim/type. Every later user gets it instantly as a cacheHit (and via the ±3-year fallback, neighboring years too) and no regeneration ever occurs. There is no admin invalidation path in this code.
- **Fix:** Before storeCachedGuide, validate the parsed guide: require steps.length >= 3 with non-empty instruction strings and a non-empty title. If invalid, return the guide to the requester (or 502) but skip the cache store so the next request regenerates.
- **Verifier:** Verified in code. parseGuideResponse (src/app/api/guide/route.ts:354-419) defaults all fields — (parsed.steps || []), instruction || '', title fallback — so a valid-JSON refusal/degenerate model response (json_object response_format guarantees parseable JSON) produces a Guide with empty steps/tools/parts and a normal-looking title. route.ts:671 stores it unconditionally via storeCachedGuide; guide-cache.ts:164-166 upserts with update:{} ('first-writer wins'), making it the permanent cached guide for that YMMT+type, served as cacheHit to all later users and to ±3 neighboring years via the nearb

### [medium] hub-chat mid-stream failure: client silently keeps the partial reply while the server never persists it — session history and what the user sees diverge
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/hub-chat/route.ts:602 (ai-flows)
- **Detail:** If the OpenAI stream dies mid-generation (the 55s AbortSignal covers the whole body read, so long answers abort midway), the server jumps to catch and sends {type:'error'} — the partial-reply persist at lines 602-612 only runs on the success path, so ChatSession is missing the assistant turn. Meanwhile the client (VehicleHub.tsx line 912) does `content: copy[idx].content || event.message` — when partial content exists the error message is discarded entirely, so the user sees a truncated answer presented as complete. Next turn, the server-built history has two consecutive user messages and the model re-answers or contradicts the text on screen. Separately, if the client disconnects mid-stream, controller.enqueue throws, the catch's send() throws again, and controller.close() in finally throws a third time → unhandled rejection noise and the persist is skipped even for fully-generated replies.
- **Fix:** Persist assistantText (when non-empty) in the finally block, appending a marker like '\n\n[reply truncated]' on the error path; wrap send() and controller.close() bodies in try/catch; on the client, when an error event arrives with partial content append a visible '— connection lost, answer may be incomplete' suffix instead of dropping the message.
- **Verifier:** Verified in code. Server (src/app/api/hub-chat/route.ts): AbortSignal.timeout(55_000) at line 556 covers the whole body read, so reader.read() throws mid-generation on long answers; the assistant-reply persist (lines 602-612) sits on the success path inside the try, so any mid-stream throw jumps to catch (615), emits {type:'error'}, and skips persistence. The divergence claim is confirmed by line 432: next turn the server rebuilds history as [...DB prevMessages, last client message] — ignoring the client's local copy — so the model sees two consecutive user messages and never sees the truncate

### [medium] Garage assistant: no rate limit, no fetch timeout, and conversationHistory forwarded to OpenAI unbounded
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/garage/assistant/route.ts:460 (ai-flows)
- **Detail:** This route has none of the protections the other AI routes have: (1) no rate limiter at all (only auth + the non-functional budget gate); (2) neither OpenAI fetch (lines 460, 530) has an AbortSignal — a hung upstream connection holds the function until the platform kills it (no maxDuration export either); (3) conversationHistory from the request body is spread into messages with no truncation, length cap, or even role validation (line 452) — every other route slices/truncates history, this one sends whatever an authed user posts, every request; (4) JSON.parse(toolCall.function.arguments) at line 503 is unguarded — a malformed tool-args string from the model 500s the whole request (the /api/chat equivalent wraps this in try/catch). Also, if the follow-up response contains further tool_calls instead of content, `followUpData.choices[0].message.content` is null and the client receives message:null.
- **Fix:** Add hubChatMinuteLimiter (or a dedicated limiter) + export const maxDuration; give both fetches AbortSignal.timeout(20_000); slice history to the last 8 messages with a ~2000-char per-message cap; wrap the args JSON.parse in try/catch returning 'Tool call failed.'; fall back to actions text when follow-up content is null.
- **Verifier:** Verified directly in C:/Users/devon/autocarecompanion/src/app/api/garage/assistant/route.ts. (1) No rate limiter: the file never imports @/lib/rate-limit, while a grep shows every other AI route (chat, hub-chat, vision, guide, drive/*) checks one; the only gates are auth() and isBudgetExceeded(), which relies on costs.ts writing to per-instance ephemeral /tmp on Vercel and so does not reliably aggregate. (2) No timeout: neither OpenAI fetch (lines 460, 530) passes an AbortSignal and there is no maxDuration export — the garage route is the only AI route absent from the AbortSignal/maxDuration g

### [medium] /api/chat creates a brand-new ChatSession row containing the full conversation history on every turn
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/chat/route.ts:1054 (ai-flows)
- **Detail:** sessionHash is regenerated per request (line 1034, generateSessionHash uses Date.now()+random) and prisma.chatSession.create (line 1054) runs on every POST — there is no sessionId reuse, unlike hub-chat. A 10-turn conversation produces 10 rows storing 1+2+...+10 copies of the messages: quadratic duplication of the same content. ChatMessageSchema puts no max length on content (chat.schema.ts line 25) and no max count on conversationHistory, so the persisted allMessages (which uses the UNtruncated history, lines 1045-1053 — the 600-800-char truncation only applies to what's sent to the model) is fully client-controlled up to the body-size limit. This is DB bloat that compounds with traffic and makes the 'interaction analysis' data set full of duplicates.
- **Fix:** Accept an optional sessionId in ChatRequestSchema and update the existing row (mirroring hub-chat's pattern), or persist only the new user+assistant pair keyed by a stable client-provided session hash. Add .max(4000) to ChatMessageSchema.content and .max(40) to conversationHistory.
- **Verifier:** Verified in C:/Users/devon/autocarecompanion/src/app/api/chat/route.ts: generateSessionHash() (lines 764-769, Date.now+Math.random) is regenerated at line 1034 on every POST, and prisma.chatSession.create (line 1054) runs unconditionally per request — ChatRequestSchema (src/schemas/chat.schema.ts:68-78) has no sessionId field and the route has no update path, unlike hub-chat (src/app/api/hub-chat/route.ts:425-450) which accepts body.sessionId, updates the existing row, and caps messages at MAX_HISTORY_MESSAGES. Persisted allMessages (route.ts:1045-1053) uses the raw client conversationHistory;

### [low] Vision route: unguarded openaiResp.json() and post-parse code can throw between quota-consume and respond, bypassing the refund invariant
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/vision/route.ts:516 (ai-flows)
- **Detail:** `const data = await openaiResp.json()` (line 516) sits outside any try/catch — a 200 response with a malformed/interrupted body throws, escapes POST, and returns a generic 500 without refundPhotoQuota, breaking the route's stated 'every failure path refunds' invariant (lines 129-131). Same exposure for attachVendorLinks (line 671) and any throw in the result-shaping block before respond(). Low likelihood per-request, but these are the only non-refunding failure paths left besides the Vercel kill covered separately.
- **Fix:** Wrap everything from openaiResp.json() through respond({ vision: result }) in a try/catch whose catch calls failWithRefund({ error: 'vision_failed', ... }, 502).
- **Verifier:** Verified in src/app/api/vision/route.ts. The route's own comment (lines 129-131) states the invariant that every post-consume failure path must call refundPhotoQuota. Quota is consumed at line 137. The try/catch at lines 488-502 covers only the fetch() call (ends when headers arrive); line 516 `const data = await openaiResp.json()` is outside any try/catch, so a 200 response with a malformed/interrupted body rejects, escapes POST, and Next.js returns a generic 500 with no refund — the credit is lost. Tellingly, the !ok branch guards its body read with `.text().catch(() => '')` at line 505, con

### [low] Model names hardcoded at 8+ call sites across 6 routes with two conflicting price tables — drift risk on the next migration
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/chat/route.ts:461 (ai-flows)
- **Detail:** 'gpt-5.4' is hardcoded in /api/chat (4 call sites: lines 461, 496, 546, 1002), /api/guide (line 318), /api/guide/help (line 80), and /api/garage/assistant (lines 467, 537); 'gpt-5.5' in /api/hub-chat (line 107) and /api/vision (env-overridable, the only one). The vision route's comment history shows three model flip-flops already (5.5 → 5.2 → 5.5) — each future change means hunting 8 sites, and a missed one silently runs a different model with different token/reasoning behavior (which is exactly how the chat route's 60s-timeout assumptions could break). Cost constants are similarly split: costs.ts uses $0.03/$0.06 per 1K while guide/route.ts line 649 uses $10/$30 per 1M.
- **Fix:** Create src/lib/ai-models.ts exporting CHAT_MODEL, GUIDE_MODEL, VISION_MODEL, HUB_MODEL (each with an env override like OPENAI_VISION_MODEL already has) plus a per-model price map consumed by both costs.ts and the guide cost estimate; replace all literals.
- **Verifier:** Verified every claim against the code. (1) 'gpt-5.4' is hardcoded at all 8 cited call sites: chat/route.ts lines 461, 496, 546, 1002 (plus logApiCost labels at 993/1012), guide/route.ts:318 (plus 646), guide/help/route.ts:80 (plus 177), garage/assistant/route.ts:467 and 537 (plus 493/559). (2) hub-chat/route.ts:107 hardcodes 'gpt-5.5' as a file constant with no env override; vision/route.ts:66 is the only env-overridable site (OPENAI_VISION_MODEL || 'gpt-5.5'). (3) The vision route comment at lines 61-65 documents the 5.5 → 5.2 → 5.5 flip-flop exactly as described. (4) The two conflicting pric

## Completeness-critic findings (spot-checked by the critic itself, not double-verified)

### [critical] Account deletion never cancels the user's active Stripe subscription — deleted users keep getting charged $9.99/mo with no way to stop it
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/account/delete/route.ts:82
- **Detail:** DELETE /api/account/delete (the GDPR Art. 17 erasure endpoint, wired to the 'Delete account' button in src/components/account/AccountPrivacyActions.tsx line 157) purges chat/notification/push rows and then calls prisma.user.delete — which erases the User row including stripeCustomerId and subscriptionId. The route imports no Stripe code and the client flow makes no prior call to /api/account/cancel-subscription. Result: a paying subscriber who deletes their account leaves an ACTIVE Stripe subscription billing $9.99/mo indefinitely; future webhook events can no longer be mapped to any user (the customer id is gone from the DB), and the user has no account or portal session left to cancel from. This is recurring unauthorized billing — chargeback/FTC click-to-cancel liability — and the route's own 90-line doc comment about GDPR never mentions Stripe.
- **Fix:** In the DELETE handler, before prisma.user.delete: fetch { subscriptionId, stripeCustomerId } for the user; if subscriptionId exists call getStripe().subscriptions.cancel(subscriptionId) (immediate cancel, since the account is being erased), and optionally stripe.customers.del(stripeCustomerId) for full PII erasure on Stripe's side; abort the account deletion with a 502 if the Stripe cancel fails so the user is never left in the billed-but-deleted state.

### [high] /api/interest email-capture writes to Vercel's read-only filesystem — every lead signup 500s in production and the email is silently lost
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/interest/route.ts:26
- **Detail:** The endpoint persists interest emails via mkdirSync/appendFileSync into process.cwd()/data. Vercel's function filesystem is read-only except /tmp (the same EROFS bug this repo already diagnosed and fixed in src/lib/costs.ts, per the project's own notes), so the write throws, the catch returns 500, and the email is never recorded anywhere — console.error logs the exception, not the address. EmailCapture.tsx line 27 posts here from the live site, so the lead-generation funnel has been collecting nothing in production.
- **Fix:** Persist to Postgres instead: add a small Prisma model (e.g. InterestEmail { id, email @unique, createdAt }) and replace the fs write with prisma.interestEmail.upsert. Also update /api/admin/data, which reads these same files, to read from the table.

### [high] /api/feedback and /api/feedback/vehicle have the same read-only-FS write — all user feedback fails with 500 in production and the content is lost
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/feedback/route.ts:42
- **Detail:** POST /api/feedback (wired to src/components/ui/FeedbackForm.tsx line 32) appendFileSync's to process.cwd()/data/feedback.jsonl, and POST /api/feedback/vehicle (wired to CustomVehicleInput.tsx line 100, read by the admin dashboard at src/app/admin/page.tsx line 172) fs.writeFile's to data/vehicle-feedback.json. Both throw EROFS on Vercel, return 500, and drop the submitted message — bug reports and AI-vehicle-parse corrections from real users have been vanishing. The admin page's vehicle-feedback view always shows an empty list because loadFeedback() swallows the read failure and returns [].
- **Fix:** Replace both file writes with Prisma inserts (e.g. a Feedback model with type/message/email/userAgent, and a VehicleParseFeedback model). Same change as costs.ts: nothing under process.cwd() is writable on Vercel.

### [high] Cron maintenance-check endpoint fails open when CRON_SECRET is unset — anyone can repeatedly trigger reminder-email blasts to every active subscriber
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/cron/maintenance-check/route.ts:28
- **Detail:** Auth is `if (cronSecret && authHeader !== ...)` — when the CRON_SECRET env var is missing the check is skipped entirely and the route is public (GET and a deliberately-added unauthenticated POST 'for manual triggers'). CRON_SECRET appears nowhere in .env or .env.local and in no other file in the repo, so it is very likely unset in production too. processMaintenanceNotifications() (src/lib/notifications.ts line 372) has no idempotency: NotificationLog rows are written but never read, so every hit re-sends the full email batch to all subscriptionStatus='active' users with due maintenance via Resend. An attacker who finds /api/cron/maintenance-check (it's in vercel.json, which is in the public repo pattern) can spam paying customers in a loop and burn Resend quota.
- **Fix:** Fail closed: `if (!cronSecret || authHeader !== `Bearer ${cronSecret}`) return 401`. Set CRON_SECRET in Vercel project env (Vercel automatically sends it for cron invocations). Remove the POST handler. Additionally, add a since-last-sent guard using NotificationLog (skip users notified for the same type in the last 20h) so even an authorized double-fire can't double-email.

### [high] Web push is a console.log stub behind a fully working subscribe UI — users grant permission, see success, are logged as notified, and never receive anything
- **Where:** C:/Users/devon/autocarecompanion/src/lib/notifications.ts:227
- **Detail:** sendPushNotification() contains 'In production, use web-push library. For now, we'll just log' — it console.logs the payload, increments `sent`, and processMaintenanceNotifications then writes a NotificationLog row with channel='push' claiming delivery. Meanwhile the client side is fully built: src/hooks/usePushNotifications.ts (lines 91/134) runs the real browser PushManager.subscribe flow and POSTs the subscription to /api/notifications/push-subscribe, which stores it and flips user.pushNotifications=true. So a paying subscriber who enables push maintenance reminders gets a permission prompt, a stored subscription, a success state, audit rows saying notifications were sent — and zero notifications, ever. Users who toggle OFF email (assuming push covers them) silently lose all reminders.
- **Fix:** Implement actual delivery with the `web-push` npm package: webpush.setVapidDetails(mailto, NEXT_PUBLIC_VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY) then webpush.sendNotification({endpoint, keys:{p256dh, auth}}, JSON.stringify(payload)) per subscription, deleting subscriptions on 404/410. Until that ships, hide the push UI and stop writing channel='push' NotificationLog rows.

### [high] Public builds API leaks VIN, current mileage, and userId for every showcased vehicle
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/builds/route.ts:32
- **Detail:** Both GET /api/builds (line 32) and GET /api/builds/[slug] (src/app/api/builds/[slug]/route.ts line 13) query prisma.vehicle with `include` but no vehicle-level `select`, so the JSON response contains every Vehicle scalar: vin (schema.prisma line 167), currentMileage, lastMileageUpdate, annualMileage, userId, isPrimary, createdAt. These are unauthenticated public endpoints with pagination — a single scripted crawl harvests the VIN + owner display name + odometer reading of every user who made their build public. Owners opted into showing their build, not their VIN; VINs enable title-history lookups, VIN cloning, and pair with the user's name from the included user object.
- **Fix:** Add an explicit select to both queries exposing only showcase-safe fields: { id, year, make, model, trim, color, nickname, imageUrl, showcaseSlug, isPublic, updatedAt } plus the existing user{name,image} and modifications selections. Never return vin, currentMileage, annualMileage, lastMileageUpdate, userId, or isPrimary on a public route.

### [medium] No rate limiting on /api/auth/forgot-password — unlimited email bombing of any address and Resend cost burn
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/auth/forgot-password/route.ts:26
- **Detail:** The handler does bcrypt-free work but sends a Resend email on every request for a registered address, with no limiter of any kind: grep for rateLimit across src/app/api/auth matches nothing, and src/middleware.ts only handles Citroën URL redirects. The repo already has a working IP limiter (src/lib/rate-limit.ts, used by the drive routes) that simply isn't applied here. An attacker can loop POSTs with a victim's email to flood their inbox from notifications@au7o.io (sender-reputation damage + Resend quota cost), and each request also invalidates the victim's previous reset token (updateMany usedAt at line 57), letting an attacker race a legitimate user's reset attempts. /api/auth/signup has the same gap (bcrypt.hash CPU burn + account spam).
- **Fix:** Apply the existing in-memory limiter (or a small Prisma-backed counter, since per-instance memory is weak on Vercel) at the top of forgot-password: e.g. max 3 requests per email per hour and 10 per IP per hour, returning the same { ok: true } body when throttled so enumeration resistance is preserved. Do the same for signup.

### [low] Coverage note: surfaces still unreviewed after this gap pass
- **Where:** C:/Users/devon/autocarecompanion/src/app/api
- **Detail:** Verified clean or already-covered during this pass: /api/auth/reset-password (hashed single-use tokens, transactional consume — solid), /api/auth/dev-reset and /api/dev/activate-subscription (correctly dead in production via NODE_ENV guard), /api/diag (harmless log sink), Sentry wiring (withSentryConfig in next.config.ts line 173 + instrumentation.ts — wired, just dormant until DSN is set), account delete CSRF (custom header forces preflight). Still NOT read by anyone: /api/vehicles and /api/maintenance CRUD ownership checks, /api/account/export contents, /api/admin/guides|recommendations|patterns|reports (only admin/data was reviewed — the others may share its missing-auth pattern), /api/known-issues/report, /api/user/secondary-email and /api/auth/checkout-signin, /api/hub-chat/session/[id] access control, and the seven remaining /api/drive/* third-party-proxy routes (poi-photo, fuel-prices, cameras, etc.) which sit unauthenticated behind the same per-instance in-memory limiters already flagged for /api/guide.
- **Fix:** Next review pass should prioritize the other /api/admin/* routes (highest likelihood of repeating the unauthenticated admin/data PII pattern) and ownership checks on /api/vehicles/[id] and /api/maintenance/[id].

## Audit: db-integrity

### [critical] 43 published issues have categories outside the enum — crashes article SSR for 32 models incl. Honda Civic, CR-V, RAV4
- **Where:** C:/Users/devon/autocarecompanion/src/app/known-issues/[slug]/page.tsx:532
- **Detail:** 43 published rows carry category values not in the 17-value issueCategorySchema enum: fuel-system (20), electronics (12), ignition (9), wheels-tires (2). They span 32 models including top-traffic SEO pages: Honda Civic (1), Honda CR-V (1), Toyota RAV4 (1), Nissan Rogue (1), Acura MDX (1), plus Saab 9-5 (4), Alfa Romeo Giulia (2), Mazda CX-60/Mazda6/Mazdaspeed6/RX-7 (2 each), Lincoln Aviator (2), Pontiac Solstice (2), Buick Lucerne (2), and 17 more. The render path is unguarded: dbRowToKnownIssue (src/lib/known-issues.ts:16) passes row.category raw (normalizeCategory at line 432 exists but is never applied), groupByCategory (page.tsx:165) groups by the raw string, then the mobile TOC does `const config = categoryConfig[category]` (page.tsx:524) and dereferences `config.icon` (line 532) — TypeError on server render. Same unguarded dereference in ArticleSidebar.tsx:35 (`config.label`) and CategorySection.tsx:71. Any article page for these 32 models that re-renders (ISR revalidate or fresh build) throws; at build time a throwing SSG page fails the whole deploy. Note: normalizeCategory's map has 'fuel_system' (underscore) but the DB rows use 'fuel-system' (hyphen), so even it would only rescue these to 'other'.
- **Fix:** One-time data fix (UPDATE "KnownIssue" SET category='fuel' WHERE category='fuel-system'; electronics->'electrical'; ignition->'engine'; wheels-tires->'suspension'); then make the code defensive: apply normalizeCategory(row.category) inside dbRowToKnownIssue at src/lib/known-issues.ts:16, add 'fuel-system','electronics','ignition','wheels-tires' to the categoryMap at line 425, and change the three render sites to `categoryConfig[category] ?? categoryConfig.other`. Also add a category whitelist check to the persist scripts that wrote these rows (_persist-known-issues-run.js path).

### [high] 161 published issues have severity 'critical' — invisible in the article issues list and badged 'Moderate'
- **Where:** C:/Users/devon/autocarecompanion/src/components/known-issues/ArticleIssuesList.tsx:90
- **Detail:** Severity distribution on published rows: medium 2067, high 1672, low 671, critical 161. The value 'critical' is outside the zod enum (knownIssue.schema.ts:72 allows only high/medium/low) and outside every UI severity map. Consequences, all verified in code: (1) ArticleIssuesList.tsx:39 initializes severityFilter to ['high','medium','low'] and line 90 drops anything not in it, and SeverityFilter.tsx offers no 'critical' option — so all 161 issues are permanently hidden from the interactive issues list on the article pages of ~110 affected models, with no way for the user to reveal them; (2) KnownIssueCard.tsx:102 `severityConfig[issue.severity] || severityConfig.medium` renders any that do surface elsewhere with the yellow 'Moderate' badge; (3) page.tsx FAQ/JSON-LD counts (lines 69, 182, 356) use `severity === 'high'` only, so these worst-severity issues are excluded from the 'rated critical' counts in the SEO FAQ text. The site's own normalizeSeverity (src/lib/known-issues.ts:443) maps critical->high, but these rows were persisted without it.
- **Fix:** One-time data fix matching the existing normalizer: UPDATE "KnownIssue" SET severity='high' WHERE severity='critical'. Then enforce it at the boundary: call normalizeSeverity(row.severity) inside dbRowToKnownIssue (src/lib/known-issues.ts:20) so future bad rows degrade safely, and add the same whitelist to the persist scripts.

### [medium] 1,381 of 4,571 published issues (30.2%) have an empty citations array; promote gate auto-passes them
- **Where:** C:/Users/devon/autocarecompanion/scripts/_promote-pending-review.js:63
- **Detail:** 1,381 published rows have citations = []. Breakdown by source: ai-researched 1,157, manual 168, nhtsa-verified 56 (nhtsa-verified rows with zero citations contradict their own provenance label). This violates the project's stated sourcing policy (cite real sources, label AI content) and is an E-E-A-T liability on a site whose revenue depends on Google organic. Root cause is structural: _promote-pending-review.js line 63 only runs the URL-liveness gate `if (cites.length > 0)` — a row with zero citations scores live=0/dead=0, `dead > live` is false, and it publishes unchallenged. Empty-citation rows are therefore the one class the quality gate can never catch.
- **Fix:** In scripts/_promote-pending-review.js, treat empty citations as a skip: after line 62 add `if (cites.length === 0) { skipped++; ...; continue; }` (or require at least one live citation to promote). For the 1,381 already-published rows, run the citation backfill (scripts/backfill-citations.js with URL validation per the May 2026 audit memory) before the next promotion cycle, prioritizing the 56 nhtsa-verified rows where real ODI/recall URLs should exist.

### [medium] 59 orphan dtcCodes on 73 published issues — several are free-text sentences that render as garbage internal links
- **Where:** C:/Users/devon/autocarecompanion/src/components/known-issues/KnownIssueCard.tsx:290
- **Detail:** Published issues reference 783 distinct dtcCodes; 59 have no row in DTCCode, and 73 published issues reference at least one orphan. Orphan list (first 30): 00448, 00522, 01130, 01155, 01203, '01203 — Electrical Connection between ABS and Instrument Cluster', 01276, 01314, 01316, 01336, 01367, '01435 Brake Pressure Sensor 1 (G201): Implausible Signal', 16487, 16500, 16502, 17114, 17125, 17664, 2E81, 2E82, 2E83, 2E84, 2E85, 377A, 65535, '65535 Internal Control Module Memory Error', B1022F0, B1DXX, 'Battery voltage low history codes', 'C-codes or U-codes in front camera/ADAS modules depending on fault'. KnownIssueCard.tsx:287-290 and the server-rendered src/app/[locale]/known-issues/[slug]/page.tsx:196 emit `<Link href={/known-issues/dtc/${code.toLowerCase()}}>` for every entry, so the free-text values become URL-encoded garbage hrefs and every orphan becomes a crawlable internal 404 in static HTML (wasted crawl budget, broken-link signals on 73 pages).
- **Fix:** Data cleanup script: for each published row, keep only dtcCodes matching /^[PBCU0-9][0-9A-F]{3,6}$/i (strip trailing prose like '01203 — Electrical...' down to the bare code, delete pure-prose entries). Render guard: in KnownIssueCard.tsx:287 and [locale]/known-issues/[slug]/page.tsx:196, only emit a Link when the code matches that pattern (render plain text otherwise), or pass the set of known DTCCode codes down and link only those.

### [medium] 227 published citations point at carcomplaints.com (historically hallucinated domain); 825 citations (14.5%) have no URL and bypass verification
- **Where:** C:/Users/devon/autocarecompanion/scripts/_promote-pending-review.js:64
- **Detail:** Across 5,688 citation entries on published rows: 227 point at carcomplaints.com (196 www + 31 m.carcomplaints.com) — the exact domain where the May 2026 audit found 984 hallucinated URLs — and 825 entries (14.5%) have no url field at all. 0 URLs fail the http-prefix check. The promote gate makes the no-URL problem self-perpetuating: line 64 of _promote-pending-review.js maps a citation without a url to `{ url: null, live: true }`, counting unverifiable citations as live. Top cited domains: nhtsa.gov 661, youtube.com 653, reddit.com 293, carcomplaints.com 196, static.nhtsa.gov 185, consumerreports.org 56, repairpal.com 42, classaction.org 40, carproblemzoo.com 37, nissan-techinfo.com 36, m.carcomplaints.com 31, lincolnforums.com 27, bimmerpost.com 25, toyotanation.com 24, hyundai-forums.com 24, topclassactions.com 24, silveradosierra.com 24, cx70forum.com 24, allpar.com 22, go-parts.com 22.
- **Fix:** Re-run the liveness check specifically against the 227 carcomplaints.com citations (they return real 404s when hallucinated) and strip/replace dead ones. In _promote-pending-review.js line 64, change URL-less citations to count as dead (`{ url: null, live: false }`) or exclude them from the live tally so they can't pad the pass score.

### [low] 2 exact-duplicate published issue pairs (same make+model+title)
- **Where:** C:/Users/devon/autocarecompanion/prisma/schema.prisma:456
- **Detail:** 2 duplicate groups among 4,571 published rows (2 redundant rows): (1) RAM 1500 Classic '5.7L Hemi Exhaust Manifold Bolt Breakage' — ids ram-1500-classic-exhaust-manifold-bolts-2019 and ram-1500-classic-exhaust-manifold-2019; (2) Audi Cabriolet 'Convertible Top Hydraulic System Failure' — ids audi-cabriolet-top-hydraulic-1994 and audi-cabriolet-top-hydraulic-failure-1994. Each pair renders as two identical cards on the same article page.
- **Fix:** Archive one row of each pair (set status='archived'), keeping the one with more citations/reportCount; merge any unique dtcCodes/recommendations into the survivor first.

### [low] 124 of 758 models (16%) have fewer than 3 published issues — thin-content article pages
- **Where:** C:/Users/devon/autocarecompanion/src/app/known-issues/[slug]/page.tsx:165
- **Detail:** 124 make+model combos out of 758 have <3 published issues, most with exactly 1, each producing a near-empty SEO article page. First 40 (all n=1): Acura TSX, Acura Vigor, BMW Z8, Buick Cascada, Buick Encore, Buick Encore GX, Buick Envision, Buick LaCrosse, Buick LeSabre, Buick Park Avenue, Buick Roadmaster, Citroen C5, CUPRA Leon, Dacia Logan, Dacia Spring, Genesis GV80 Coupe, Infiniti EX35, Infiniti Q50, Infiniti Q70, Lexus ES300, Lexus LC, Lexus SC400, Lincoln Continental, Lincoln LS, Lincoln MKX, Mercedes-Benz EQS SUV, Mercury Cougar, Mitsubishi 3000GT, Mitsubishi Montero Sport, Opel Crossland, Opel Grandland, Opel Vivaro, Peugeot 508, Polestar Polestar 1, Polestar Polestar 3, Pontiac Aztek, Pontiac Bonneville, Pontiac Firebird, Pontiac G8, Pontiac Grand Am. Note the recently-added makes (Buick, Pontiac, Lincoln, Opel, Dacia, Polestar) dominate the list.
- **Fix:** Fold these into the quarterly review queue: either backfill each to 3+ issues via the research pipeline, or exclude models with <2 published issues from generateStaticParams/sitemap until they have enough content to stand as an article.

### [low] 22 of 749 DTCCode rows have descriptions under 100 chars — thin standalone DTC pages
- **Where:** C:/Users/devon/autocarecompanion/prisma/schema.prisma:507
- **Detail:** DTCCode table has 749 rows; 22 have empty/short (<100 char) descriptions, each backing a thin /known-issues/dtc/[code] page: P006B (90), P0309 (77), P0310 (78), P0311 (75), P0312 (75), P0A7A (96), P2003 (47), P2BA4 (91), P2BA5 (45), C1136 (65), C1140 (66), C1148 (91), C1260F0 (67), U1118 (83), U112100 (64), 002F (96), 01315 (65), 01447 (72), 01772 (93), 02250 (43), plus 2 more. The worst (02250 at 43 chars, P2BA5 at 45, P2003 at 47) are one-sentence stubs.
- **Fix:** Run the existing DTC enrichment script (scripts/_persist-dtc-enrich.js pipeline) scoped to these 22 codes to bring descriptions to parity with the other 727.

### [low] 59 published rows carry undocumented source value 'ai-researched-v2'
- **Where:** C:/Users/devon/autocarecompanion/src/schemas/knownIssue.schema.ts:86
- **Detail:** Source distribution: ai-researched 4,625, ai-researched-v2 59, manual 190, nhtsa-verified 67. 'ai-researched-v2' is not in the zod source enum (knownIssue.schema.ts:86 allows nhtsa-verified/recall-related/ai-researched/manual) nor in the prisma schema comment (schema.prisma:480). Any provenance-based logic (badges, audits, the quarterly re-research selector) that string-matches 'ai-researched' will silently miss these 59 rows.
- **Fix:** Either normalize the 59 rows to source='ai-researched' (UPDATE WHERE source='ai-researched-v2'), or if v2 is meaningful pipeline metadata, add it to the zod enum and the prisma comment so downstream code knows it exists.

### [low] 6 published issues have empty symptoms arrays (all Tesla Semi / Rivian EDV)
- **Where:** C:/Users/devon/autocarecompanion/prisma/schema.prisma:469
- **Detail:** 6 published rows have symptoms=[]: tesla-semi-touchscreen-flicker-shutdown-forcing-drivers-to-pull-over, rivian-edv-front-upper-control-arm-steering-knuckle-fastener-under-torq, tesla-semi-parking-brake-valve-module-fails-to-engage-rollaway-risk, tesla-semi-lithium-ion-battery-thermal-runaway-after-crash-50-000-gallo, tesla-semi-heavy-load-range-collapse-500-mile-rating-only-light-freight, tesla-semi-megacharger-network-gaps-strand-trucks-off-corridor. Empty symptoms removes these from symptom-matching (src/lib/symptoms.ts) and leaves an empty Symptoms section on the cards.
- **Fix:** Backfill symptoms for these 6 rows (they are commercial-vehicle entries from one batch — the persisting script likely dropped the field); add a symptoms.length >= 1 check to the persist validation.

### [low] DB snapshot
- **Where:** C:/Users/devon/autocarecompanion/scripts/_tmp_review_audit.js:1
- **Detail:** Status: published 4,571; archived 370; pending_review 0 (total 4,941). Source: ai-researched 4,625; ai-researched-v2 59; manual 190; nhtsa-verified 67. Severity (published): medium 2,067; high 1,672; low 671; critical 161. Published field validity: invalid severity 0 (after counting 'critical' as valid per spec); invalid category 43 (fuel-system 20, electronics 12, ignition 9, wheels-tires 2); empty citations 1,381; empty years 0; years outside 1980-2027: 0; costLow>costHigh 0; empty symptoms 6. DTC: 783 distinct codes referenced by published issues, 59 orphans (no DTCCode row), 73 issues affected; DTCCode table 749 rows, 22 with <100-char descriptions. Duplicates: 2 groups / 2 redundant rows. Citations: 5,688 total entries on published rows; 825 with no url field; 0 non-http URLs; 227 carcomplaints.com (196 www + 31 m.). Top domains: nhtsa.gov 661, youtube.com 653, reddit.com 293, carcomplaints.com 196, static.nhtsa.gov 185. Coverage: 758 make+model combos, 124 with <3 published issues. Note: published count (4,571) is well above the 3,987 recorded in project memory on 2026-05-23.
- **Fix:** No action — reference snapshot for this audit (2026-06-10). Audit script left at scripts/_tmp_review_audit.js for cleanup by the orchestrator.

## Audit: coverage-gaps

### [high] Brazil's best-selling vehicles entirely absent despite new pt-BR pages
- **Where:** C:\Users\devon\autocarecompanion\src\data\i18n\pt-br.json
- **Detail:** The site just shipped pt-BR locale pages, but the DB has zero rows for the vehicles Brazilians actually search: Fiat Strada (Brazil's #1 selling vehicle since 2021), Chevrolet Onix (#1 car 2016-2021), Hyundai HB20 (perennial top-3), VW Gol (best-seller for 27 straight years, enormous used parc), VW Polo/T-Cross/Virtus/Saveiro/Nivus, Fiat Toro/Argo/Mobi/Uno/Palio, Hyundai Creta, Renault Kwid, Honda City. Verified via groupBy on published KnownIssue: none of these models exist under any make. The current pt-BR content (Silverado 1500, Tacoma, Grand Wagoneer, Colorado) covers vehicles that are not even sold in Brazil — pt-BR organic traffic will bounce or never arrive.
- **Fix:** Run a known-issues research batch for the BR top-10 parc: Fiat Strada, Chevrolet Onix, Hyundai HB20, VW Gol, VW Polo, VW T-Cross, Fiat Uno, Fiat Argo, Hyundai Creta, Renault Kwid (8-12 issues each, BR-market engines: Fire/Firefly, 1.0 TSI/200 TSI, Kappa 1.0). Publish en pages first, then add them to the next pt-BR translation run.

### [high] Toyota Hilux absent — best-selling pickup in LatAm and a global search staple
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** Toyota has 38 models in the DB including US-only trucks (Tacoma 24, Tundra 8, T100 5) but no Hilux. Hilux is the best-selling pickup in Brazil/Argentina/LatAm and among the most-searched vehicles globally ('hilux problems', '1kd injector failure', 'hilux dpf'). With pt-BR and es pages now live, this is the single highest-demand missing nameplate for those locales, and it also has meaningful EU/AU English demand.
- **Fix:** Add Toyota Hilux with 10+ issues: 1KD-FTV injector seal/piston cracking, DPF blockage on 2GD-FTV, AdBlue/SCR faults, leaf-spring recall (2016+), front diff whine, injector rattle on 1GD. Include it in the next pt-BR and es translation batches.

### [high] Infiniti Q50 has 1 published issue; G35/G37 have 3 each
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** DB groupBy shows Infiniti Q50 = 1 published issue, G35 = 3, G37 = 3, Q60 = 2, while low-volume FX35/FX45/FX50/M35/M37 each have 5-6. The Q50 was Infiniti's best-selling vehicle 2014-2024 and has famous, heavily-searched problems (Direct Adaptive Steering failures, VR30DDTT turbo oil-supply, dual-screen InTouch failures, CVT on 2.0t). G35/G37 are massive used-market/enthusiast search targets (oil galley gasket, timing chain, steering lock). A flagship page with 1 issue ranks poorly and converts nobody.
- **Fix:** Research batch: bring Q50 to 10+ issues (DAS, VR30 turbos, InTouch, fuel pump TSB), G35/G37 to 8+ (oil consumption, timing chain tensioner, EPS lock module, radiator/transmission cross-contamination on G35).

### [high] Tesla Model 3 (4 issues) and Model X (1) nearly empty while Tesla Semi has 5
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** Tesla coverage: Model 3 = 4, Model S = 4, Model X = 1, Cybertruck = 1, Model Y = 8, Semi = 5. Model 3 is the best-selling EV in US history (>1M US units) with enormous problem-search volume; Model X has the most notorious reliability reputation (falcon doors, MCU). The Semi — a class-8 truck nobody privately owns — has more issues than Model X and Cybertruck combined. This is exactly the audience for the $9.99 US subscription.
- **Fix:** Seed Model 3: front upper control arm clunk, heat pump/octovalve failure (recall 22V-077), HW3 computer failures, screen yellowing, MCU eMMC wear, charge port pin burn. Model X: falcon door sensors, MCU1 eMMC (recall), air suspension, half-shaft shudder. Cybertruck: tonneau motor, accelerator pedal recall.

### [medium] High-volume US models stuck under 5 issues: Lexus ES/NX, Buick Encore, Palisade, GMC Terrain/Acadia/Yukon, Highlander
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** Published counts from DB: Lexus ES = 4 (+orphan 'ES300' = 1) and NX = 2, UX = 2 — ES and NX are Lexus's two best sellers. Buick Encore = 1 (Buick's #1 seller 2013-2021, known turbo/cluster issues), LaCrosse = 1. Hyundai Palisade = 4 (flagship 3-row; wiring-harness chafing, oil consumption, windshield cracking are heavily searched). GMC Terrain = 4 (GMC's best seller; 2.4L oil consumption class action), Acadia = 4 (shift-to-park is one of the most-searched GM problems), Yukon = 4. Toyota Highlander = 6 and Chevy Blazer = 3 round out the list. Each is a six-figure-monthly 'problems' search family served by a near-empty page.
- **Fix:** Next quarterly batch targets: Encore (turbo failure, cluster, PCV), Terrain (oil consumption, timing chain), Acadia (shift to park, transmission shudder), ES (2007-12 oil line, melting dash), NX (12.3in screen, fuel pump recall), Palisade (harness, oil), Highlander (oil cooler line Gen2, blower, tailgate), Blazer (9-speed shudder, screen).

### [medium] Research effort inverted vs demand inside model families (Yukon XL 22 vs Yukon 4, Sierra 3500HD 24 vs 2500HD 5)
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** The 'beef up thin articles' pass overshot on low-demand variants while their high-volume siblings stayed thin: GMC Yukon XL = 22 vs Yukon = 4 (Yukon outsells XL ~2:1); Sierra 3500HD = 24 vs 2500HD = 5 (2500HD is the volume HD trim); Jeep Grand Wagoneer = 24 (low-volume $90k+ SUV) vs Wagoneer = 4; MINI GP = 18 (limited run of ~3,000 cars) vs Cooper = 5; Kia K5 = 24 vs Forte = 4 (Forte outsold K5 most years); Mazda CX-60 = 19 (not sold in US) vs CX-5 = 12 (Mazda's US best seller). Search demand follows sales volume; the deep articles sit on the wrong slugs.
- **Fix:** Rebalance the next batch toward the high-volume sibling: Yukon, Sierra 2500HD (L5P/LML issues), Wagoneer, MINI Cooper/Cooper S (timing chain death rattle, carbon buildup), Kia Forte (theta/nu engine, IVT), CX-5. Where issues genuinely overlap (Yukon/Yukon XL share everything), copy applicable issues across both models instead of leaving one empty.

### [medium] Minivan and Prius coverage thin relative to their search demand
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** Toyota Sienna = 7 (vs Odyssey 15) despite huge family-buyer search volume (sliding door cable failure, 2GR oil line, AWD PTU, hybrid 2021+ issues). Toyota Prius = 7 issues covering ~25 years and 5 generations — Gen3 head gasket/EGR clogging, hybrid battery failure, brake actuator, and combination-meter failure are each large standalone search families; Prius problems queries skew high-intent DIY. Chrysler Town & Country = 5 also thin for its parc.
- **Fix:** Expand Sienna to 12+ (door cables, oil cooler line, run-flat wear, hybrid water pump) and Prius to 14+ with generation-tagged years (Gen2 2004-09: combination meter, MFD; Gen3 2010-15: head gasket, EGR, brake booster recall; Gen4/5: heat pump, 12V). Generation tagging matters because searches are year-specific.

### [medium] Scion absent as a make; Mercury reduced to a single 1-issue model
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\make\[make]\page.tsx
- **Detail:** No Scion rows exist in the DB (verified via per-make groupBy — 49 makes, no Scion). Scion tC/xB/xD/FR-S sold ~1M US units 2004-2016 and their owners are exactly the DIY demographic ('scion tc problems' remains a steady search). Mercury exists but with only Cougar = 1 issue; Grand Marquis — the model with by far the largest surviving parc and active owner community (shared Panther-platform demand with Crown Victoria, which has 5) — is absent entirely. Memory notes already list Mercury/Oldsmobile as known domestic gaps; Scion is not on that list.
- **Fix:** Add Scion tC (2AZ-FE oil consumption, water pump), xB, FR-S (FA20 valve springs recall, fuel pump chirp), and Mercury Grand Marquis (intake manifold cracking, air suspension, blend door) — most Panther issues can be adapted from Crown Victoria rows.

### [medium] EU staple superminis thin or absent: VW Polo missing entirely, Corsa 3, 208 3, Astra 4, Clio 4
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** For a site now publishing pt-BR/es content with EU makes onboard (Opel, Peugeot, Renault, SEAT, Skoda, Dacia): VW Polo is absent from the DB — a perennial top-5 EU seller that is ALSO a top-10 Brazil seller, making it a double gap. Opel Corsa = 3, Astra = 4, Peugeot 208 = 3 (current EU #1 in several years), Renault Clio = 4, Megane = 3. Fiat Punto (huge EU+BR legacy parc) is absent. These thin pages exist but cannot compete for 'problems/probleme/problemas' queries.
- **Fix:** Add VW Polo (8+ issues: 1.2 TSI timing chain, DQ200 DSG mechatronic, EA211 water pump) and Fiat Punto; expand Corsa (1.2/1.4 timing chain), 208 (1.2 PureTech wet belt — one of the highest-volume EU engine problems right now), Clio (DCT, 0.9 TCe) to 8+ each.

### [medium] Sandero/Duster exist only under Dacia — LatAm searches use the Renault badge
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** DB has Dacia Sandero = 4 and Dacia Duster = 4, but in Brazil and all of LatAm these are sold and searched as RENAULT Sandero/Duster ('renault sandero problemas'). A pt-BR user searching the Renault badge will never land on /known-issues/dacia-sandero, and the pt-BR locale route can only translate models that exist. Renault Logan and Kwid (both BR volume sellers) are absent under any badge.
- **Fix:** Add Renault-badged Sandero, Duster, Logan, Kwid rows (clone/adapt the Dacia issue content where mechanically identical — same platforms), so both badge searches have a landing page; include the Renault-badged versions in pt-BR/es translation sets.

### [medium] Symptom pages cover EU diesel warning lights but miss the US brand-specific warning messages
- **Where:** C:\Users\devon\autocarecompanion\src\data\symptoms.json
- **Detail:** The 70 existing slugs include an EU-diesel cluster (epc-light-on, dpf-light-on, glow-plug-light-flashing, adblue-def-warning-message) but none of the equivalent US-brand messages, despite the subscription being US-only: 'Service StabiliTrak' (GM — one of the highest-volume automotive warning searches), 'Engine Power Reduced' (GM), Ford wrench light, Honda D light flashing (transmission), Toyota 'Check VSC System'/'VSC TRAC' light, BMW 'Drivetrain Malfunction', GM 'Steering Assist is Reduced, Drive with Care', Honda 'Emissions System Problem'. Each maps directly to existing known-issue and DTC content for internal linking.
- **Fix:** Add 8 symptom entries: service-stabilitrak-light, engine-power-reduced-message, ford-wrench-light-on, honda-d-light-flashing, toyota-vsc-light-on, bmw-drivetrain-malfunction-warning, steering-assist-reduced-message, honda-emissions-system-problem — each cross-linked to the relevant make pages and DTC codes (C0561, P0700, P2138, P0420).

### [medium] High-volume generic symptom searches with no page: no heat, vibration when accelerating, stuck in park, power windows
- **Where:** C:\Users\devon\autocarecompanion\src\data\symptoms.json
- **Detail:** Verified against the 70 existing slugs — missing, in rough demand order: heater blowing cold air / no heat (huge seasonal volume; AC-not-cold exists but heat does not), car vibrates/shudders when accelerating (distinct from the existing highway-speed shake page; points to inner CV/motor mounts/torque-converter shudder content already in the DB), shifter stuck in park (brake-shift interlock — high volume, easy DIY answer), burning rubber smell (burning-oil and electrical exist; rubber does not), power window won't roll up, blower motor / AC fan not working, water leaking into floorboard when it rains, brake lights not working, speedometer/gauges not working, service 4WD light (matches the site's heavy truck audience).
- **Fix:** Add ~10 entries: heater-blowing-cold-air, car-vibrates-when-accelerating, shifter-stuck-in-park, burning-rubber-smell, power-window-wont-go-up, blower-motor-not-working, water-leaking-into-car-floor, brake-lights-not-working, speedometer-not-working, service-4wd-light-on.

### [medium] P0113 and P0705 have no DTC reference row and no page; all other probed high-volume codes are covered
- **Where:** C:\Users\devon\autocarecompanion\src\lib\dtc-codes.ts:84
- **Detail:** Probed 64 high-search-volume codes against the 749-row DTCCode table and the dtcCodes arrays of published issues. Present with live pages: P0420/P0430, P0171/P0174, full P0300-P0306 misfire family, full EVAP family (P0440/P0441/P0442/P0446/P0455/P0456/P0457), P0128, P0299, P0700, U0100/U0101/U0121/U0140, plus C0035/C1201/B1342 — coverage is genuinely strong. Missing entirely (no ref row AND zero linked issues, so per getAllDTCSlugs/the 404 rule no page can exist): P0113 (IAT sensor circuit high — common Ford/Nissan, large search family) and P0705 (transmission range sensor / PRNDL input — very common, high-intent searches).
- **Fix:** Insert DTCCode rows for P0113 and P0705, and tag at least one published KnownIssue with each (e.g. Nissan IAT/MAF issues for P0113; Honda/Ford range-sensor issues for P0705) so the page-generation gate at dtc-codes.ts:84 produces the pages.

### [medium] Slug collision: Chevrolet 'Trailblazer' and 'TrailBlazer' are two different vehicles merged onto one page
- **Where:** C:\Users\devon\autocarecompanion\src\lib\known-issues.ts:59
- **Detail:** DB contains both Chevrolet|Trailblazer (4 issues, the 2021+ subcompact crossover) and Chevrolet|TrailBlazer (5 issues, the 2002-2009 GMT360 SUV). makeSlug() lowercases, so both produce 'chevrolet-trailblazer'; getAllKnownIssueSlugs() (line 75) emits two entries with the same slug (duplicate sitemap URLs), parseSlug() resolves to whichever comes first, and the case-insensitive issue fetch merges a 4.2L I6 body-on-frame SUV and a 1.3L turbo crossover into one article with one title — confusing content for a six-figure-monthly search family, and the page's years/issues mix two unrelated platforms.
- **Fix:** Disambiguate the model strings in the DB (e.g. rename the GMT360 rows to model 'TrailBlazer (2002-2009)' or 'TrailBlazer EXT-era' producing a distinct slug, with a redirect), or split rendering by year-range; also dedupe getAllKnownIssueSlugs() output so the sitemap never carries the same URL twice.

### [medium] Translated model set was picked by issue-count, not locale demand — Corolla untranslated while Grand Wagoneer is
- **Where:** C:\Users\devon\autocarecompanion\src\data\i18n\es.json
- **Detail:** pt-br.json and es.json contain the identical 15 models — the top-15 by issue count — including Jeep Grand Wagoneer (a low-volume $90k+ US luxury SUV not sold in either market), Silverado 1500, Tacoma, and Colorado. Meanwhile Toyota Corolla (18 issues already in the DB; the best-selling car in Brazil's history and a top seller in Mexico and with US Spanish speakers) is untranslated. For es-MX/es-US the highest-demand candidates with existing English content are all missing: VW Jetta (16 issues — the iconic Mexican car), Nissan Sentra (16), Nissan Versa (7, Mexico's #1 seller for years), Ford F-150 (23, top es-US demand), Ford Explorer (18). The two locales should not share one list.
- **Fix:** Next translation batches, diverging per locale — pt-BR: Corolla, Corolla Cross, Jeep Compass, Jeep Renegade, Ford Ranger, Fiesta (+Hilux/Onix/HB20 once created); es: Corolla, Jetta, Sentra, Versa, F-150, Explorer. Drop nothing existing; just stop mirroring the sets.

### [medium] Ford Focus/Fiesta thin despite PowerShift DPS6 being one of the most-searched car problems of the decade
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** Ford Focus = 6 published issues, Fiesta = 5, Fusion = 5. The 2011-2016 Focus/Fiesta DPS6 PowerShift dual-clutch (shudder, clutch pack failure, TCM failure — subject of a massive class action and buyback program) generates sustained, high-intent search volume that a 5-6 issue page cannot capture; Fusion adds swollen lug nuts, 1.5/1.6 EcoBoost coolant intrusion, and steering recalls. These are exactly the high-parc, out-of-warranty owners the subscription targets.
- **Fix:** Expand Focus and Fiesta to 10+ issues each with the DPS6 problems split into distinct entries (shudder/TCM/clutch, with the extended-warranty program details), and Fusion to 10+ (coolant intrusion, lug nuts, EPAS).

### [low] Junk values polluting KnownIssue.dtcCodes and non-OBD2 rows in the DTCCode table
- **Where:** C:\Users\devon\autocarecompanion\src\lib\dtc-codes.ts:305
- **Detail:** 59 distinct non-code strings sit in published issues' dtcCodes arrays, e.g. 'BATTERY VOLTAGE LOW HISTORY CODES', 'CODE 22', '65535 INTERNAL CONTROL MODULE MEMORY ERROR', 'B1DXX', 'IMPLAUSIBLE WHEEL-SPEED SIGNAL CODES (SINGLE-WHEEL)'. The regex in getAllDTCMakeSlugs (line 305, '^[CPUB]?[0-9A-F]{4,5}$') filters most, but '65535', '2E81'-'2E85', '377A', '16487' pass it and generate static params for per-make DTC pages that then 404 (no ref row). Separately, 7 DTCCode reference rows are not OBD-II codes at all ('002F', VAG 5-digit codes like '01315'/'16566', 'ECM-940A'), which pollute related-code cross-links.
- **Fix:** One-time cleanup: strip free-text entries from dtcCodes arrays (move prose to symptoms/description), and either delete the 7 non-OBD2 ref rows or namespace them (e.g. 'VAG-16566') so they cannot enter the generic /dtc/[code] route; tighten the line-305 regex to '^[PCBU][0-9][0-9A-F]{3}$'.

### [low] Ford Super Duty content split across duplicate model names dilutes a flagship truck query
- **Where:** C:\Users\devon\autocarecompanion\src\app\known-issues\[slug]\page.tsx
- **Detail:** DB has both 'F-250' (5 issues) and 'F-250 Super Duty' (7 issues) as separate models producing two competing pages (ford-f-250 vs ford-f-250-super-duty) for one search family — and 6.0L/6.7L Powerstroke problem searches are enormous. Same pattern: 'Dodge Ram' (2) vs 'Dodge Ram 1500' (5), and Mercedes 'SLK-Class' (6) / 'SLK/SLC' (3) / 'SLC' (5) three-way split. Split pages halve internal-link equity and can cannibalize each other in SERPs.
- **Fix:** Merge each pair to one canonical model string ('F-250 Super Duty', 'Ram 1500', 'SLK-Class'), move the issues, and 301 the orphaned slug via next.config redirects (the Citroën soft-404 redirect work is the existing pattern to copy).

## Audit: photo-pipeline

### [low] Capture model: DiagnosisSample stores metadata-only rows; every field enumerated
- **Where:** C:/Users/devon/autocarecompanion/prisma/schema.prisma:598
- **Detail:** DiagnosisSample (schema.prisma:598-642) is the only photo-diagnosis capture table. Fields: id; userId (nullable, FK User onDelete:Cascade); anonId (nullable, always null today — route gate is authed-only); denormalized vehicle context year/make/model/trim (all nullable); mode ('photo'|'video', default 'photo'); primaryCategory (e.g. 'rotor'); primaryPartName; dtcCodes String[] (currently always written as [] — diagnosis-capture.ts:133 hardcodes empty); confidence Float; vehicleMatch ('confident'|'uncertain'|'likely_mismatch'); diagnosisJson Json (PII-scrubbed subset); image columns pre-created but unused in Phase 0.0: imageStored (default false), storageKey (null), cropApplied (false), exifStripped (default true), contentType, byteSize; consent audit: consentSource ('account_toggle'|'per_upload_checkbox'), consentVersion (default 'v1'), createdAt. Indexes: userId, [make,model], primaryCategory, createdAt. The diagnosisJson payload built at src/lib/diagnosis-capture.ts:105-121 contains: summary (scrubbed), confidence, difficulty, estimatedTimeMinutes, parts[] (max 12: role, category, name, oemPartNumbers max 6, confidence, visibleInPhoto), relatedIssueIds (max 8). Deliberately excluded: vehicleMatchNote, user caption, audio transcript (diagnosis-capture.ts:61-68).

### [low] Consent gating: three-state per-upload signal with account-toggle fallback; capture is photo-mode + authed + car-related only
- **Where:** C:/Users/devon/autocarecompanion/src/lib/diagnosis-capture.ts:84
- **Detail:** Consent resolution (diagnosis-capture.ts:82-95): perUploadConsent '1' → store with consentSource='per_upload_checkbox'; '0' → never store (overrides account opt-in); null → fall back to User.visualDatasetOptIn (schema.prisma:59, default false, with visualDatasetConsentAt timestamp at :60 stamped by /api/account/preferences route.ts:92-93). The per-upload value comes from the multipart 'keepPhoto' field parsed at api/vision/route.ts:240-242. Capture is invoked fire-and-forget via Next after() only when ALL of: userId set, mode==='photo', result.isCarRelated, vehicleMatch !== 'likely_mismatch' (api/vision/route.ts:782-791) — video diagnoses are NEVER captured. UI: the keepPhoto checkbox exists ONLY in DiagnoseFlowClient.tsx (state at :62, appended at :145 — '1'/'0' for signed-in users only, anon never sends it; copy at :604-606 says 'No photo is kept'). The VehicleHub upload paths (VehicleHub.tsx:325 photo, :528 video) do NOT append keepPhoto, so hub uploads always hit the null branch and depend on the account toggle (AccountPrivacyActions.tsx:103). Separate from consent: GDPR Art. 21 aiProcessingOptOut (schema.prisma:50) short-circuits the whole vision call via checkAiGate (api/vision/route.ts:104-105, src/lib/ai-gate.ts:31-58). PII scrub before persist: VIN/email/phone regexes in scrubPII (diagnosis-capture.ts:70-79).

### [low] Image bytes are never stored — buffer lifecycle is in-memory base64 → OpenAI → GC
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/vision/route.ts:281
- **Detail:** Buffer lifecycle in POST /api/vision: multipart File(s) parsed at :191-226 (photo: single 'image' field; video: up to 6 'frames' + optional 'audio'); each image converted to a base64 data URL in memory at :281-288 (Buffer.from(arrayBuffer) → data:<type>;base64,...); data URLs are embedded in the OpenAI Responses API body as input_image entries at :463-465 and POSTed at :489-497; no write to disk, /tmp, or object storage anywhere in the route; after the fetch the buffers go out of scope. DiagnosisSample rows are written with imageStored:false, storageKey:null, cropApplied:false, exifStripped:true (diagnosis-capture.ts:137-142). Video audio is forwarded once to OpenAI whisper-1 (:258-267) and only the transcript string (capped 1000 chars, :269) survives in the response — transcript is returned to the client (:738) but never persisted (excluded from capture). Client-side video frames/audio are in-memory Blobs only (video-extract.ts:14-16).

### [low] AI output: structured JSON with per-part identification, confidence, and known-issue IDs; persisted twice with different linkage
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/vision/route.ts:735
- **Detail:** The model (gpt-5.5 via OpenAI /v1/responses, reasoning.effort 'low', json_object format — :60-66, :467-484) returns per the schema at :400-431: summary, confidence 0-1, isCarRelated, vehicleMatch + vehicleMatchNote, primaryPartId, identifiedParts[] (each: id, role primary/consumable/fastener/related, category from a 31-value enum :625-630, name, spec, position, per-part confidence, visibleInPhoto, brand, oemPartNumbers[], aftermarketPartNumbers[], searchQuery, estimatedPriceUsd, notes), toolsNeeded, difficulty, estimatedTimeMinutes, warnings, relatedKnownIssueIds. Known-issue linking happens TWO ways: (a) prompt-side — top 6 published KnownIssues for the YMMT injected with their DB ids (:298-326) and the model echoes matches in relatedKnownIssueIds, which are re-validated against the DB at :721-731 and returned as relatedIssues [{id,title,severity}]; (b) persistence-side — relatedIssueIds (max 8) stored inside DiagnosisSample.diagnosisJson (diagnosis-capture.ts:118-120) — a JSON array, NOT an FK relation. Vendor links attached server-side via attachVendorLinks (:671). Separate persistence path: /api/diagnose/seed stores the FULL VisionResult on ChatSession.diagnosis and inside ChatSession.messages, linked to a real Vehicle row + userId (api/diagnose/seed/route.ts:189-197) — so vehicle/user linkage exists via ChatSession, while DiagnosisSample has userId + denormalized YMMT but no Vehicle FK.

### [low] EXIF, downscale, format, and size facts: EXIF orientation baked but metadata stripped only as a canvas side-effect; HEIC handled client-side only; 10MB server cap
- **Where:** C:/Users/devon/autocarecompanion/src/lib/downscale-image.ts:21
- **Detail:** Client downscale (downscale-image.ts): MAX_DIMENSION 1920px long edge, JPEG quality 0.85, files under 700KB skipped (no re-encode → original EXIF metadata travels to the server intact for small jpeg/png, :40); EXIF orientation is baked into pixels via createImageBitmap({imageOrientation:'from-image'}) (:91-93) with HTMLImageElement fallback (:101-109); canvas re-encode to image/jpeg (:70-76) incidentally strips all EXIF for re-encoded files — there is no explicit EXIF-stripping library anywhere (grep 'exif' across src/ matches only comments + the exifStripped DB flag). HEIC/HEIF: accepted by the client matcher (:27) and always re-encoded when decodable (iOS Safari) (:35-40); on browsers that can't decode HEIC the original falls through and the SERVER rejects it — server accepts only image/(jpeg|png|webp|gif) (api/vision/route.ts:222-223 photo, :205-206 frames) with an iPhone 'Most Compatible' hint in the 415 message. Server size caps: MAX_IMAGE_BYTES 10MB per image (:67), video frames capped at 6 (:200), audio 200B-20MB (:210); Vercel's 4.5MB body cap is the real-world constraint cited at downscale-image.ts:5-7. Video frames: FRAME_MAX_DIMENSION 1024px, JPEG q0.82, 4 frames default, 30s max clip (video-extract.ts:19-21, :44).

### [low] Reusable infra: no pgvector, no blob storage SDK, no stored embedding vectors; offline embedding script + relatedIssueIds column are the only vector-adjacent assets
- **Where:** C:/Users/devon/autocarecompanion/scripts/compute-issue-embeddings.js:44
- **Detail:** pgvector: NOT enabled — schema.prisma datasource (lines 8-10) has no extensions block and the generator has no postgresqlExtensions previewFeature; compute-issue-embeddings.js:9-10 states explicitly 'We don't have pgvector enabled and don't want to require a Postgres extension upgrade'. Embeddings: the only embedding code is the offline script (text-embedding-3-small, 1536 dims, batch 100 — :44-46) which computes pairwise cosine in JS and persists ONLY the top-K neighbor IDs to KnownIssue.relatedIssueIds String[] (schema.prisma:490; script :249-258) — the raw vectors are discarded, no embedding column exists on any model. Blob storage: package.json has NO @vercel/blob, @supabase/supabase-js, or any storage SDK; the only Supabase usage is Postgres via pg/PrismaPg (.env.local:23-24 DATABASE_URL/DIRECT_URL pooler endpoints); BLOB_READ_WRITE_TOKEN absent from env. The account-delete route comment (src/app/api/account/delete/route.ts:15-17) anticipates 'blob objects must be purged from storage FIRST' for the future Phase 0.1 pipeline but no storage code exists. DiagnosisSample's storageKey/contentType/byteSize/cropApplied columns (schema.prisma:624-629) are pre-provisioned and entirely unused.

### [low] Gap list: minimum missing pieces between today and photo → embedding → nearest labeled failures → candidate known issues
- **Where:** C:/Users/devon/autocarecompanion/src/app/api/vision/route.ts:782
- **Detail:** Facts defining the gap: (1) No image persistence — bytes are discarded after the OpenAI call (route.ts:281-288); Phase 0.1 storage target (private bucket + crop) is design-only (schema comment schema.prisma:594-597, MEMORY plan says Supabase Storage/Blob, neither SDK installed). (2) No image-embedding capability — the only embedder is the offline text script (compute-issue-embeddings.js:44); no vision/CLIP-style embedding code, no runtime embedding endpoint, no vector column on DiagnosisSample or KnownIssue, no pgvector extension, so no nearest-neighbor query path exists in the DB. (3) No labeled-image corpus — DiagnosisSample rows carry labels (primaryCategory, primaryPartName, diagnosisJson.parts, relatedIssueIds) but zero pixels (imageStored hardcoded false, diagnosis-capture.ts:138); video-mode diagnoses are excluded from capture entirely (route.ts:782). (4) Known-issue linkage in the capture is a JSON string array inside diagnosisJson, not a queryable relation or column (diagnosis-capture.ts:118-120) — slicing samples by known-issue requires JSON traversal. (5) dtcCodes on DiagnosisSample is always [] (diagnosis-capture.ts:133) — no DTC signal flows into samples despite the column existing. (6) Account-deletion blob purge (delete/route.ts:15-17) and data-export inclusion of stored images are unimplemented prerequisites the code comments already flag for the moment bytes are kept. (7) Existing pieces that ARE ready to reuse: consent plumbing end-to-end (keepPhoto field → perUploadConsent → consentSource audit), pre-provisioned image columns (storageKey/contentType/byteSize/cropApplied/exifStripped), EXIF-stripping-by-re-encode in the client downscaler, the KnownIssue prompt-injection + relatedKnownIssueIds validation loop (route.ts:298-326, :721-731), and the YMMT + primaryCategory indexes on DiagnosisSample for pre-filtering candidates before any vector math.

## Unverified (verifier died on session limit — re-check before acting)

- [api-security] Unauthenticated admin route burns Anthropic budget AND overwrites known-issue content for any issue ID
- [api-security] Unauthenticated admin guides route allows mass deletion and content tampering of cached repair guides
- [api-security] Entire /api/admin/** surface has no auth — relies purely on URL obscurity; no admin role exists in the app
- [api-security] hub-chat session continuation has no ownership check — lets a caller write into and exfiltrate another user's private chat
- [api-security] Anonymous AI endpoints can run up the founder's OpenAI bill — IP gate is spoofable, in-memory limits don't share state, and the budget cap is non-functional on serverless
- [api-security] API cost tracking + hard budget cap write to ephemeral /tmp on Vercel, so the cap never enforces and the admin dashboard is blank
- [api-security] Stripe webhook has correct signature verification but no event-replay / idempotency guard
- [payments] Account takeover: checkout-signin grants a 30-day session to anyone holding a paid Stripe checkout session ID
- [payments] Plus and Pro photo/video quotas are identical; TIER_LIMITS.diagnosesPerWeek is never enforced
- [payments] DB subscriptionTier desyncs from Stripe — webhook derives tier only from metadata, never from the price
- [payments] Webhook updateMany on stripeCustomerId can no-op silently, leaving canceled/past_due users with paid access
- [payments] Chat quota is consumed before the AI call and never refunded on failure
- [payments] Pro->Plus downgrade is blocked by the region gate but the UI offers it, so grandfathered/non-US Pro users get a 403
- [payments] trialing subscribers are entitled by resolveTier but treated as free in chat/vision/maintenance

Stats: {"rawFindings":28,"confirmed":14,"refuted":14}
